## Status: DONE

### Completed
- Created `bundle/direct/dstate/migrate.go` with versioned state migration system
- Migration runs automatically after state is loaded, before any operations
- Bumped `currentStateVersion` from 1 to 2
- V1→V2 migration converts permissions state from old format (`"permissions"` key + `"permission_level"` field) to new format (`"_"` key + `"level"` field)
- Old struct types copied directly from main branch into `migrate.go` for migration purposes
- Handles version 0 (absent `state_version` field) same as version 1
- Removed `UnmarshalJSON` from `PermissionsState` — migration now happens at state level
- Removed `PermissionLevel` field from `StatePermission` (no longer needed)
- Added unit tests for migration (4 test cases)
- Updated acceptance test `permission_level_migration` to use truly old format with `"permissions"` key
- Updated all acceptance test outputs for state_version 2
- All unit tests pass (4619 tests)
- All acceptance tests pass
