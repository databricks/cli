trace $CLI bundle init default-bare --config-file ./input.json --output-dir output

cd output

[ -d "src" ] || exit 1
[ -d "resources" ] || exit 1

trace $CLI bundle validate -t dev

# Do not affect this repository's git behaviour #2318
mv .gitignore out.gitignore
rm -r .databricks
