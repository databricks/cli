resources:
  jobs:
    wait_for_run_sensor:
      name: "[example] wait_for_run_sensor"

      tasks:
        - task_key: wait_for_run_sensor
          environment_key: my_lakeflow_integrations_environment
          python_operator_task:
            main: my_lakeflow_integrations.examples.wait_for_run_sensor.WaitForRunSensor
            parameters:
              - name: "run_id"
                value: "CHANGEME"

      environments:
        - environment_key: my_lakeflow_integrations_environment
          spec:
            environment_version: "5"
            dependencies:
              - ../dist/*.whl
