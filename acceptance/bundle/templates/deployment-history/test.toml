Env.DATABRICKS_BUNDLE_INIT_DEPLOYMENT_HISTORY = "true"

# What is under test is what `bundle init` writes, so one engine is enough.
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct"]

# The generated project sets experimental.deployment_history; DMS=true permits it so
# the validate step below loads instead of rejecting it.
EnvMatrix.DMS = ["true"]
