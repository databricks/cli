# This test deploys on terraform and migrates to direct. Terraform deployments are not supported
# with deployment-history recording, so it only runs without it.
EnvMatrix.DMS = [""]

RecordRequests = true
IncludeRequestHeaders = ["User-Agent"]

# The restored terraform deploy leaves terraform provider artifacts behind.
Ignore = [".databricks"]
