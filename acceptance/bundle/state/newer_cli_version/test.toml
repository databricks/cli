Ignore = [".databricks"]

# The warning is emitted when reading the direct engine's resources.json.
# This fixture seeds file-backed state; DMS mismatch behavior is covered under bundle/dms.
EnvMatrix.DMS = [""]
