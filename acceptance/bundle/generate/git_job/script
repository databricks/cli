trace $CLI_BINARY bundle generate job --existing-job-id 1234 --config-dir . --key out

mkdir subdir1
cd subdir1
trace $CLI_BINARY bundle generate job --existing-job-id 1234 --config-dir . --key out
diff.py out.job.yml $TESTDIR/out.job.yml
rm out.job.yml
