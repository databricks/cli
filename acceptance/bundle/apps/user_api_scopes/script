cleanup() {
    trace $CLI bundle destroy --auto-approve
}
trap cleanup EXIT
envsubst < databricks.yml.tmpl > databricks.yml
trace $CLI bundle plan
trace $CLI bundle deploy &> out.deploy.$DATABRICKS_CLI_DEPLOYMENT.txt
