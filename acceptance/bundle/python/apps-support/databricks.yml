bundle:
  name: my_project

sync: {paths: []} # don't need to copy files

experimental:
  python:
    resources:
      - "resources:load_resources"
    mutators:
      - "mutators:update_app"

resources:
  apps:
    my_app_1:
      name: "My App"
      description: "My first app"
