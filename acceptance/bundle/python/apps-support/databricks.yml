bundle:
  name: my_project

sync: {paths: []} # don't need to copy files

python:
  resources:
    - "resources:load_resources"
  mutators:
    - "mutators:update_app"

resources:
  apps:
    my_app_1:
      name: "my_app_1"
      description: "My app"
      source_code_path: "./app1"
