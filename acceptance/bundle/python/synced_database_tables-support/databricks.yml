bundle:
  name: my_project

sync: {paths: []} # don't need to copy files

python:
  resources:
    - "resources:load_resources"
  mutators:
    - "mutators:update_synced_database_table"

resources:
  synced_database_tables:
    my_synced_table_1:
      name: "main.default.my_synced_table_1"
