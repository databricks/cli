Cloud = false # tests don't interact with APIs

# external_locations are only supported in the current version of the wheel
EnvMatrix.PYDAB_VERSION = ["current"]

# external_locations are only supported on the direct deployment engine
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct"]
