bundle:
  name: my_project

sync: {paths: []} # don't need to copy files

experimental:
  python:
    resources:
      - "resources:load_resources"
    mutators:
      - "mutators:update_pipeline"

resources:
  pipelines:
    my_pipeline_1:
      name: "My Pipeline 1"
