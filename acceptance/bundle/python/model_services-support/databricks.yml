bundle:
  name: my_project

sync: {paths: []} # don't need to copy files

python:
  resources:
    - "resources:load_resources"
  mutators:
    - "mutators:update_model_service"

resources:
  model_services:
    my_model_service_1:
      parent: "schemas/main.default"
      model_service_id: "my_model_service_1"
      comment: "My model service"
