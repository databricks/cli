bundle:
  name: test-bundle-$UNIQUE_NAME

resources:
  experiments:
    foo:
      name: /Users/$CURRENT_USER_NAME/test-experiment-$UNIQUE_NAME
      permissions:
        - level: CAN_READ
          group_name: users
