bundle:
  name: test-bundle-$UNIQUE_NAME

resources:
  apps:
    foo:
      name: app-$UNIQUE_NAME
      source_code_path: ./app
      permissions:
        - level: CAN_USE
          group_name: users
