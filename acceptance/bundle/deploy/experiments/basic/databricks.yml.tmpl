bundle:
  name: experiment-basic-$UNIQUE_NAME

resources:
  experiments:
    my_experiment:
      name: "/Users/${CURRENT_USER_NAME}/test-experiment-${UNIQUE_NAME}"
      description: "Test experiment for bundle deployment"
