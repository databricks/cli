bundle:
  name: notebook-gap

resources:
  jobs:
    etl:
      name: etl
      tasks:
        - task_key: run_etl
          spark_python_task:
            python_file: ./src/etl.py
          environment_key: default
      environments:
        - environment_key: default
          spec:
            client: "2"
