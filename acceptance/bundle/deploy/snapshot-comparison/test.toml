Cloud = true

RecordRequests = true

Ignore = [
    ".databricks",
    "databricks.yml",
    "tmp.*",
    "bundle1",
    "bundle2"
]

[Env]
DATABRICKS_BUNDLE_ENABLE_EXPERIMENTAL_YAML_SYNC = "true"

[EnvMatrix]
DATABRICKS_BUNDLE_ENGINE = ["terraform"]
