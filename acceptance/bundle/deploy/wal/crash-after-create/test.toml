# WAL recovery after simulated crash. Job was created but state wasn't finalized.
# Deploy should recover job from WAL and update it.

[[Server]]
Pattern = "POST /api/2.2/jobs/reset"
Response.Body = '{}'

[[Server]]
Pattern = "GET /api/2.2/jobs/get"
Response.Body = '{"job_id": 1001, "settings": {"name": "test-job-a"}}'
