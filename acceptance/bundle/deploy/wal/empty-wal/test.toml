# Empty WAL file should be moved to .wal.corrupted and deploy should proceed normally.

[[Server]]
Pattern = "POST /api/2.2/jobs/create"
Response.Body = '{"job_id": 1001}'

[[Server]]
Pattern = "GET /api/2.2/jobs/get"
Response.Body = '{"job_id": 1001, "settings": {"name": "test-job"}}'

[[Repls]]
Old = '-rw[^\s]+\s+\d+\s+[^\s]+\s+[^\s]+\s+\d+\s+[A-Z][a-z]+\s+\d+\s+\d+:\d+'
New = '[FILE_INFO]'
