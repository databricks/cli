# WAL with corrupted MIDDLE entry - valid entries are recovered and corrupted entries are skipped.

# Since valid entries are recovered, jobs will be updated (not created fresh).
[[Server]]
Pattern = "POST /api/2.2/jobs/create"
Response.Body = '{"job_id": 9999}'

[[Server]]
Pattern = "GET /api/2.2/jobs/get?job_id=9999"
Response.Body = '{"job_id": 9999, "settings": {"name": "fresh-job"}}'
