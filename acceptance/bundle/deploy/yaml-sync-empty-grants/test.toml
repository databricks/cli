# The YAML-sync state upload only runs for the terraform engine
# (it converts terraform state to the direct format), so there is
# no direct-engine variant of this test.
[Env]
DATABRICKS_BUNDLE_ENABLE_EXPERIMENTAL_YAML_SYNC = "true"

[EnvMatrix]
DATABRICKS_BUNDLE_ENGINE = ["terraform"]
