trace $CLI bundle plan -o json > tmp.plan.json

jq '.cli_version = "1.2.3"' tmp.plan.json > plan.json

trace $CLI bundle deploy --plan plan.json
rm plan.json
print_requests.py //jobs | nostamp
trace $CLI bundle destroy --auto-approve
rm out.requests.txt
