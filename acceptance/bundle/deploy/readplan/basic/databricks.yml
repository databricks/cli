bundle:
  name: test-bundle

# The raw plans deploy --plan reads live in the bundle root; keep them out of the file count.
sync:
  exclude:
    - tmp.plan_*.json

resources:
  jobs:
    job:
      name: test-job
