bundle:
  name: test-bind-recreate

resources:
  pipelines:
    foo:
      name: test-pipeline
      storage: /new/storage/path
      libraries:
        - notebook:
            path: ./nb.sql

targets:
  default:
    bind:
      pipelines:
        foo:
          id: "PLACEHOLDER_PIPELINE_ID"
