Cloud=true
Local=false

Badness = "the newly initialized bundle from the 'mlops-stacks' template contains two validation warnings in the configuration"

EnvMatrix.DATABRICKS_CLI_BUNDLE_ENGINE = ["terraform"] # uses 'experiments' resource

Ignore = [
    "config.json"
]

[[Repls]]
Old = "aws|azure|gcp"
New = "[CLOUD_ENV_BASE]"
