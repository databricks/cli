trace $CLI bundle deploy

trace $CLI bundle summary

alert_id=$(trace $CLI bundle summary --output json | jq -r '.resources.alerts.myalert.id')
trace $CLI alerts-v2 get-alert $alert_id

trace $CLI bundle destroy --auto-approve

trace $CLI bundle summary
