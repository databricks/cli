Local = false
Cloud = true

RecordRequests = false
RequiresUnityCatalog = true
CloudEnvs.gcp = false # lakebase is not available in GCP as of July 2025

Ignore = [
    "databricks.yml",
]

EnvMatrix.DATABRICKS_CLI_DEPLOYMENT = ["direct-exp"]
