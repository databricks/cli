bundle:
  name: shell-bash

artifacts:
  my_artifact:
    type: cmd
    build: |
      if defined CMDCMDLINE (
          echo shell is cmd.exe
      )
      echo multiline scripts should work >> out.shell.txt
      echo %CD% >> out.shell.txt
