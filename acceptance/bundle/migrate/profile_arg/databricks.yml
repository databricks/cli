bundle:
  name: migrate-basic-test

resources:
  jobs:
    test_job:
      name: "Test Migration Job"
      tasks:
        - task_key: "main"
          notebook_task:
            notebook_path: "./notebook.py"

targets:
  dev:
    default: true
  prod:
    resources:
      schemas:
        test_schema:
          catalog_name: mycat
          name: myschema
