bundle:
  name: test-bundle

include:
  - targets/*.yml
  - resources/*.yml

variables:
  mode:
    default: development
  default:
    default: false

targets:
  also_default:
    mode: development
    default: true
    workspace:
      host: https://example.com
