bundle:
  name: test-bundle

resources:
  pipelines:
    my_pipeline:
      configuration:
        spark.sql.ansi.enabled: "true"
      name: test-pipeline
      libraries:
        - file:
            path: "./intentionally-missing.py"
