bundle:
  name: test-bundle

workspace:
  root_path: /Workspace/Users/base-root

targets:
  dev:
    workspace:
      # Null on a scalar field: normalization drops it, so the base root_path
      # survives in the merged config. When null-merge semantics change this
      # field should become absent (or empty) in the output.
      root_path:

resources:
  jobs:
    my_job:
      name: my job
