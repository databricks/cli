print_requests() {
    jq --sort-keys 'select(.method != "GET" and (.path | contains("/pipeline"))) | (.body.name // empty)' < out.requests.txt
    rm out.requests.txt
}

$CLI bundle plan -o json | nostamp.py > out.plan_create.$DATABRICKS_BUNDLE_ENGINE.json
trace $CLI bundle deploy
trace print_requests
$CLI bundle plan -o json | nostamp.py > out.plan_skip.$DATABRICKS_BUNDLE_ENGINE.json
trace print_requests
