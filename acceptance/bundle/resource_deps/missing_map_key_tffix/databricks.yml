bundle:
  name: state

resources:
  jobs:
    test:
      name: "test"
      tasks:
        - task_key: "test-task"
          new_cluster:
            custom_tags:
              ResourceClass: SingleNode
    bar:
      # TF-specific fixes: tasks -> task; new_cluster -> new_cluster[0]
      name: "test ${resources.jobs.test.task[0].new_cluster[0].custom_tags.missing_tag}"
