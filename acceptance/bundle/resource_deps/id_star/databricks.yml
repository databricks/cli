bundle:
  name: test-bundle

resources:
  jobs:
    a:
      name: aa
      description: aa_desc
    b:
      name: bb
      description: prefix ${resources.jobs.a.id}
    c:
      name: cc
      description: prefix ${resources.jobs.a.id}
    d:
      name: dd
      description: prefix ${resources.jobs.a.id}
    e:
      name: ee
      description: prefix ${resources.jobs.a.id}
