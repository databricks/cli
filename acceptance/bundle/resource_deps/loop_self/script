title "Badness: validate could detect this and fail as well"
$CLI bundle validate
musterr $CLI bundle plan &> out.plan.$DATABRICKS_CLI_BUNDLE_ENGINE.txt
musterr $CLI bundle deploy &> out.deploy.$DATABRICKS_CLI_BUNDLE_ENGINE.txt
