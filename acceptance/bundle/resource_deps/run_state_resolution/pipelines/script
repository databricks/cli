trace $CLI bundle deploy --select pipelines.foo --auto-approve
# bundle run resolves ${resources.*} only within the resource being run, so unrelated_job's
# reference to the not-deployed bar is left untouched and no longer breaks the run.
trace $CLI bundle run foo
