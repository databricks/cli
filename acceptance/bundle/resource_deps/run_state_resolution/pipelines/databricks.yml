bundle:
  name: test-bundle

resources:
  pipelines:
    foo:
      name: foo
      libraries:
        - file:
            path: pipeline_file.py
  jobs:
    bar:
      name: bar
    unrelated_job:
      name: unrelated_job
      tasks:
        - task_key: run_bar
          run_job_task:
            job_id: ${resources.jobs.bar.id}
