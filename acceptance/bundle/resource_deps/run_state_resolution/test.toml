# These tests exercise `bundle run`, which (unlike validate/deploy) resolves
# ${resources.*} references against deployment state. --select is direct-engine only.
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct"]
RecordRequests = false
