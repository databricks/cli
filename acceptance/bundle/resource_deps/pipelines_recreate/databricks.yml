bundle:
  name: test-bundle

resources:
  pipelines:
    foo:
      name: pipeline foo
      storage: dbfs:/my-storage
  jobs:
    bar:
      name: job bar
      description: depends on foo id ${resources.pipelines.foo.id}
