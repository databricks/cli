bundle:
  name: test-bundle

resources:
  pipelines:
    src:
      name: source pipeline
      clusters:
        - label: mycluster
          num_workers: 1
  jobs:
    dst:
      # TF path: "cluster" (singular) → DABs: "clusters"
      name: ${resources.pipelines.src.cluster[0].label}
