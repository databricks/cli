
=== depends_on: only the real reference creates an edge
>>> jq .plan | map_values(.depends_on) plan.json
{
  "resources.jobs.bar": null,
  "resources.jobs.foo": [
    {
      "node": "resources.jobs.bar",
      "label": "${resources.jobs.bar.id}"
    }
  ]
}

=== Payload: escaped stays literal, real is resolved
Warn: Plan was created with CLI version [CLI_VERSION] but current version is [CLI_VERSION]
Error: this plan does not record deployment history, but the target now does; run 'bundle plan' again


Exit code: 1
