RecordRequests = true

# Direct engine only, where the classification lives. Guards the backend_defaults rule on
# the telemetry_config subtree: without it the remote-only config is planned as an update
# and DoUpdate patches it away.
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct"]
