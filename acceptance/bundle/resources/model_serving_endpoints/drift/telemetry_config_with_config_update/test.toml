RecordRequests = true

# Direct engine only, as with the sibling drift tests. Guards the wait in DoUpdate: without
# it this deploy fails on the telemetry patch with a 409.
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct"]
