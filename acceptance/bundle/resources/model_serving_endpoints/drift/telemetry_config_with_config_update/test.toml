RecordRequests = true

# Direct engine only, as with the sibling drift tests. Guards the wait in DoUpdate: the
# telemetry API rejects an endpoint still applying an earlier update, so without the wait
# this deploy fails on the telemetry patch with a 409.
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct"]
