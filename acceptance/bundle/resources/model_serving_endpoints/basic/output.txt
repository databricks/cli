
>>> [CLI] bundle plan -o json

>>> [CLI] bundle deploy -qq

=== Print the GET endpoint details
{
  "name": "[ENDPOINT_NAME_1]",
  "creator": "[USERNAME]"
}

>>> [CLI] bundle plan -o json

>>> [CLI] bundle deploy -qq

=== Print the GET endpoint details
{
  "name": "[ENDPOINT_NAME_2]",
  "creator": "[USERNAME]"
}

>>> [CLI] bundle destroy --auto-approve
The following resources will be deleted:
  delete resources.model_serving_endpoints.my_endpoint

All files and directories at the following location will be deleted: /Workspace/Users/[USERNAME]/.bundle/acc-[UNIQUE_NAME]/default

Deleted model_serving_endpoints.my_endpoint
Deleted model_serving_endpoints.my_endpoint.permissions
Destroy: 1 deleted
