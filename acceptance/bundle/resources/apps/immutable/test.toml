Local = true
Cloud = false # Temporary disable cloud tests until the API is fully available
RecordRequests = true

# immutable_folder only works with the direct engine.
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct"]

Ignore = [
    "databricks.yml",
    ".databricks",
    ".venv",
    "script",
    "*.pyc",
]

# Normalize the content-addressed snapshot hash so the test doesn't need
# to be updated whenever the bundle content changes.
[[Repls]]
Old = '[0-9a-f]{64}'
New = '[SNAPSHOT_HASH]'
