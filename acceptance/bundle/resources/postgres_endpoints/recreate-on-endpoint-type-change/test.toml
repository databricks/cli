# Test both engines to investigate the difference
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct", "terraform"]

[[Repls]]
# Normalize TraceId in error messages (run before default NUMID replacement at Order 9-10)
Old = 'TraceId: [a-f0-9]+'
New = 'TraceId: [TRACE_ID]'
Order = 1
