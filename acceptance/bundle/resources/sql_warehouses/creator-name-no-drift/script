cleanup() {
    trace $CLI bundle destroy --auto-approve
}
trap cleanup EXIT

trace $CLI bundle deploy
trace $CLI bundle plan
trace $CLI bundle plan -o json > out.plan.json

trace jq '.plan["resources.sql_warehouses.example_sql_warehouse"].changes.creator_name' out.plan.json
rm out.plan.json
