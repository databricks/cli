# `bundle unbind` does not yet drop the resource from what the deployment metadata service
# reports, so the plan after the unbind still sees the role as tracked.
EnvMatrix.DATABRICKS_BUNDLE_RECORD_DEPLOYMENT_HISTORY = [""]
