bundle:
  name: change-parent-path-$UNIQUE_NAME

resources:
  dashboards:
    my_dashboard:
      display_name: test dashboard
      file_path: ./dash.lvdash.json
      warehouse_id: $TEST_DEFAULT_WAREHOUSE_ID
      parent_path: $PARENT_PATH
