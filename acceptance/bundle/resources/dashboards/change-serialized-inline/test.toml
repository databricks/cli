# Local only: the real Lakeview API pretty-prints serialized_dashboard and returns a
# differently-shaped etag, which the recorded plan golden can't match.
Cloud = false

Ignore = [
    "databricks.yml",
]

EnvMatrix.READPLAN = ["", "1"]
