
=== Deploy an experiment whose config declares no trace_location
>>> [CLI] bundle deploy -qq

=== An out-of-band job bound a UC trace location; GetExperiment now echoes it
>>> [CLI] experiments get-experiment [NUMID]
{
  "name": "/Users/[USERNAME]/uc-tracing-experiment",
  "lifecycle_stage": "active",
  "trace_location": {
    "uc_trace_location": {
      "catalog": "main",
      "effective_table_prefix": "exp_[NUMID]_traces",
      "schema": "traces"
    }
  }
}

=== BUG (SUP-37168): re-plan with unchanged config recreates the experiment
=== trace_location is immutable and was set out-of-band, so the config-absent
=== remote value forces delete + create - dropping the UC trace location.
>>> [CLI] bundle plan
recreate experiments.my_experiment

Plan: 1 to add, 0 to change, 1 to delete, 0 unchanged

=== Deploy applies the recreate: the experiment is deleted and re-created
>>> [CLI] bundle deploy -qq

=== BUG: the config still matches nothing changed, yet plan recreates again
=== (the recreate never converges - every deploy destroys and re-creates)
>>> [CLI] bundle plan
recreate experiments.my_experiment

Plan: 1 to add, 0 to change, 1 to delete, 0 unchanged

>>> [CLI] bundle destroy --auto-approve
The following resources will be deleted:
  delete resources.experiments.my_experiment

All files and directories at the following location will be deleted: /Workspace/Users/[USERNAME]/.bundle/experiment-uc-tracing-recreate/default

Destroy: 1 deleted
