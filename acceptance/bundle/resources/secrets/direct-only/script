title "Deploy should fail in terraform mode"
trace $CLI bundle deploy --var secret_value=test-value 2>&1 | contains.py \
  "Secret resources are only supported with direct deployment mode" \
  "DATABRICKS_BUNDLE_ENGINE"
