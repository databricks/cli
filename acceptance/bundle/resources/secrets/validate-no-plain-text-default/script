title "Deploy should fail when variable used for secret has a default value"
trace $CLI bundle deploy 2>&1 | contains.py \
  "Variable used for secret value must not have a default value" \
  "default value is stored in plain text"
