bundle:
  name: test-bundle

resources:
  catalogs:
    test_catalog:
      name: ${workspace.resource_prefix}_catalog

  schemas:
    test_schema:
      name: ${workspace.resource_prefix}_schema
      catalog_name: ${resources.catalogs.test_catalog.name}

  secrets:
    secret1:
      catalog_name: ${resources.catalogs.test_catalog.name}
      schema_name: ${resources.schemas.test_schema.name}
      name: test_secret
      value: "plain-text-secret-not-allowed"
      comment: "This should fail validation"
