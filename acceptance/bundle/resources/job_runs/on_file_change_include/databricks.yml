bundle:
  name: job-runs-on-file-change-include

include:
  - resources/job_run.yml

resources:
  jobs:
    my_job:
      name: my-job
      tasks:
        - task_key: main
          notebook_task:
            notebook_path: /Workspace/test
