# Deploy both by re-planning and from a plan saved on disk, so resource refs
# in on_value_change survive plan serialization.
EnvMatrix.READPLAN = ["", "1"]

Ignore = ["tmp.plan.json"]

[[Repls]]
Old = '[0-9a-f]{64}'
New = '[VALUE_HASH]'
Order = 1
Distinct = true
