# The on_file_change content hash contains digit runs that the parent `\d{8,}`
# rule would mangle. Order=1 runs before it; Distinct keeps different hashes distinct.
Ignore = ["tmp.plan.json"]

[[Repls]]
Old = '[0-9a-f]{64}'
New = '[FILE_HASH]'
Order = 1
Distinct = true
