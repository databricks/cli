RecordRequests = false
Ignore = [".databricks"]

# Mask hashes before the parent's `\d{8,}` rule; Distinct keeps them distinguishable.
[[Repls]]
Old = '[0-9a-f]{64}'
New = '[FILE_HASH]'
Order = 1
Distinct = true
