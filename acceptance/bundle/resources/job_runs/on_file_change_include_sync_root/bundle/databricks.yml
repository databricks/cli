bundle:
  name: job-runs-on-file-change-include-sync-root

include:
  - resources/job_run.yml

sync:
  paths:
    - .
    - ../shared.txt

resources:
  jobs:
    my_job:
      name: my-job
      tasks:
        - task_key: main
          notebook_task:
            notebook_path: /Workspace/test
