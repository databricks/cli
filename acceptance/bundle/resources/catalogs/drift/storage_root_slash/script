echo "*" > .gitignore

title "Initial deployment"
trace $CLI bundle deploy

title "Plan is a no-op even though UC stripped the trailing slash from storage_root"
trace $CLI bundle plan | contains.py "Plan: 0 to add, 0 to change, 0 to delete, 1 unchanged"

title "The storage_root slash change is skipped as normalization (confirms the matched rule)"
trace $CLI bundle plan --output json | nostamp | jq '.plan[].changes'

title "Redeploy is a no-op (catalog is neither deleted nor recreated)"
trace $CLI bundle deploy
trace print_requests.py //unity | gron.py | contains.py '!json.method = "DELETE"'
