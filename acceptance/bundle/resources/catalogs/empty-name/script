# The deploy fails, but only after the bundle files are uploaded, so the bundle
# name carries $UNIQUE_NAME to keep concurrent cloud legs off each other's root
# path and let the run-wide sweeper find what is left behind.
envsubst < databricks.yml.tmpl > databricks.yml

trace musterr $CLI bundle deploy
