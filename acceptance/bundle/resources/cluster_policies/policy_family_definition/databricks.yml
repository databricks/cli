bundle:
  name: cluster_policy_family

resources:
  cluster_policies:
    # Authored from a policy family with no definition. The backend computes the
    # definition from the family and returns it on read; the config leaves it empty.
    family_policy:
      name: my_family_policy
      policy_family_id: personal-vm
