bundle:
  # Unique bundle + policy name so this coexists with basic/ on a shared cloud workspace.
  name: test_cluster_policy_oob

resources:
  cluster_policies:
    test_cluster_policy:
      name: my_cluster_policy_oob
      definition: '{"spark_version":{"type":"fixed","value":"13.3.x-scala2.12"}}'
