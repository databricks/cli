# Local only: simulates an out-of-band delete with a fixed resource name and no
# cloud-valid config, so it can't run against a real workspace.
Cloud = false

RecordRequests = false

Ignore = [
    ".databricks",
]
