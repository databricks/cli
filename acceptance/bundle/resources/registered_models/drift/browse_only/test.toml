RecordRequests = true

# Terraform does not model browse_only as an output-only field the same way, so
# scope this regression to the direct engine where the classification lives.
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct"]
