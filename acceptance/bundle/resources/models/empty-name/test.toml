Local = true
# Run against a real workspace too, so the golden keeps the message the fake
# returns honest: the fake is only useful here if it matches what MLflow says.
Cloud = true
RecordRequests = false
Ignore = [".databricks"]

# Both engines reach the create call and surface the backend message, so both are
# worth covering; terraform wraps it in its own output, hence the per-engine goldens.
