bundle:
  name: test-bundle

resources:
  pipelines:
    foo:
      configuration:
        spark.sql.ansi.enabled: "true"
      name: foo
      permissions:
        - level: CAN_VIEW
          user_name: viewer@example.com
