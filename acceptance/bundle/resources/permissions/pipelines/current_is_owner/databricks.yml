bundle:
  name: test-bundle

resources:
  pipelines:
    foo:
      configuration:
        spark.sql.ansi.enabled: "true"
      name: foo
      permissions:
        # explicit IS_OWNER matching username, not changed
        - level: IS_OWNER
          user_name: tester@databricks.com
