bundle:
  name: test-bundle

resources:
  pipelines:
    foo:
      name: foo
      # An explicit empty list is managed: FixPermissions adds the default IS_OWNER for the current user.
      permissions: []
