trace $CLI bundle validate -o json | jq .resources.genie_spaces.foo.permissions
rm "$OUT_REQUESTS"

$CLI bundle plan -o json | nostamp > out.plan.$DATABRICKS_BUNDLE_ENGINE.json

rm "$OUT_REQUESTS"
trace $CLI bundle deploy
# Genie space IDs are random; normalize them in the recorded requests.
replace_ids.py
print_requests.py //permissions > out.requests.deploy.$DATABRICKS_BUNDLE_ENGINE.json

trace $CLI bundle destroy --auto-approve
print_requests.py //permissions > out.requests.destroy.$DATABRICKS_BUNDLE_ENGINE.json
