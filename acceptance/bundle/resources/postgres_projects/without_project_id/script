envsubst < databricks.yml.tmpl > databricks.yml

trace $CLI bundle validate

# Deploy output differs between engines, so capture to per-engine file only
$CLI bundle deploy > out.deploy.txt 2>&1 || true
