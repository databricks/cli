envsubst < databricks.yml.tmpl > databricks.yml

cleanup() {
    trace errcode $CLI bundle destroy --auto-approve > out.destroy.txt 2>&1
}
trap cleanup EXIT
trace errcode $CLI bundle deploy --force-lock > out.deploy.txt 2>&1
