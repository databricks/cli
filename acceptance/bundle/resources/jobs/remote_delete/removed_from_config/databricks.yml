bundle:
  name: test-bundle

resources:
  jobs:
    foo:
      name: foo

  pipelines:
    bar:
      configuration:
        spark.sql.ansi.enabled: "true"
      name: bar
      libraries:
        - file:
            path: ./bar.py
