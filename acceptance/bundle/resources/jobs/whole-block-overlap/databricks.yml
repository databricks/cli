bundle:
  name: test-bundle

resources:
  jobs:
    my_job:
      name: test-job
      schedule:
        quartz_cron_expression: "0 0 12 * * ?"
        timezone_id: UTC
      tasks:
        - task_key: main
          notebook_task:
            notebook_path: /nb
