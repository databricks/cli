# The drift-suppression under test is a direct-engine feature (bundle/direct), so restrict the
# inherited engine matrix to direct. Cloud = true adds a real-workspace run; every test here
# also runs locally against the testserver.
Cloud = true

RecordRequests = false

Ignore = [
    "databricks.yml",
]
