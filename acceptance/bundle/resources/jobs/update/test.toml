EnvMatrix.READPLAN = ["", "1"]
# `bundle deploy --plan` applies pre-saved state; the stamp isn't written into the plan,
# so the next plan reports it as a change. Recording skipped until stamp reaches saved plans.
EnvMatrix.DATABRICKS_BUNDLE_RECORD_DEPLOYMENT_HISTORY = [""]
