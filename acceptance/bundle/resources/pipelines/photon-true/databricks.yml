bundle:
  name: photon-true

resources:
  pipelines:
    my:
      configuration:
        spark.sql.ansi.enabled: "true"
      name: test-pipeline
      photon: true
      libraries:
        - file:
            path: "./foo.py"
