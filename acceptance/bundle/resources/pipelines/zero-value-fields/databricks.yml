bundle:
  name: zero-value-fields

resources:
  pipelines:
    my:
      configuration:
        spark.sql.ansi.enabled: "true"
      name: test-pipeline
      photon: false
      serverless: false
      continuous: false
      development: false
      libraries:
        - file:
            path: "./foo.py"
