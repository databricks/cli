trace $CLI bundle deploy > out.deploy.txt 2>&1
trace errcode jq -s '.[] | select(.method == "POST" and (.path | contains("/pipelines")))' "$OUT_REQUESTS" | nostamp > out.requests.txt 2>&1
rm "$OUT_REQUESTS"
