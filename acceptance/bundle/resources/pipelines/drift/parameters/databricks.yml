bundle:
  name: pipeline-parameters

resources:
  pipelines:
    my:
      configuration:
        spark.sql.ansi.enabled: "true"
      name: test-pipeline
      parameters:
        my_param: my_value
      libraries:
        - file:
            path: "./foo.py"
