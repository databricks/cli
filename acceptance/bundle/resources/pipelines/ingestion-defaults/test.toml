Local = false
Cloud = true
RecordRequests = true

Badness = "Terraform engine still injects edition/channel via provider-level SetDefault (resource_pipeline.go:253); companion fix: terraform-provider-databricks#5783."

Ignore = [
    "databricks.yml",
]

[EnvMatrix]
DATABRICKS_BUNDLE_ENGINE = ["direct", "terraform"]
