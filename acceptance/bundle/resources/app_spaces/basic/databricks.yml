bundle:
  name: test-bundle

resources:
  app_spaces:
    mykey:
      name: myspacename
      description: my_space_description
      resources:
        - name: my-serving-endpoint
          serving_endpoint:
            name: my-endpoint
            permission: CAN_QUERY
        - name: my-warehouse
          sql_warehouse:
            id: warehouse-id-1
            permission: CAN_USE
