bundle:
  name: vector-search-endpoints-91238912

# workspace:
#   root_path: ~/.bundle/

resources:
  vector_search_endpoints:
    test_endpoint_version1:
      name: test-vs-endpoint-version1
      endpoint_type: STORAGE_OPTIMIZED # STANDARD is the only valid one during validation, but STORAGE_OPTIMIZED should also be added
      # min_qps: -1
  # vector_search_indexes:
  #   test_index:
  #     name: workspace.default.my_vector_search_index_6563
  #     endpoint_name: ${resources.vector_search_endpoints.test_endpoint.name}
  #     primary_key: continent
  #     index_type: DELTA_SYNC
  #     delta_sync_index_spec:
  #       source_table: workspace.default.covid_monthly_summary
  #       pipeline_type: TRIGGERED

targets:
  dev:
    default: true
