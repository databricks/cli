envsubst < databricks.yml.tmpl > databricks.yml

cleanup() {
    trace $CLI bundle destroy --auto-approve
    rm "$OUT_REQUESTS"
}
trap cleanup EXIT

trace $CLI bundle deploy
print_requests.py //cluster > out.requests.json

title "Cluster should exist after bundle deployment:\n"
CLUSTER_ID=`read_id.py test_cluster`
$CLI clusters get "${CLUSTER_ID}" | gron.py | grep json.gcp_attributes > out.result.txt
