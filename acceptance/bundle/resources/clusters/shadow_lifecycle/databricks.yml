bundle:
  name: test-bundle

sync:
  paths: []

resources:
  clusters:
    the_cluster:
      cluster_name: test-cluster
      spark_version: 15.4.x-scala2.12
      node_type_id: i3.xlarge
      num_workers: 1
      lifecycle:
        prevent_destroy: true
