targets:
  development:
    resources:
      pipelines:
        my_pipeline:

          # Append library; the path is resolved relative to this file's directory.
          libraries:
            - file:
                path: src/file2.py

            - notebook:
                path: src/notebook2.py
