bundle:
  name: config-remote-sync-telemetry-error

resources:
  jobs:
    foo:
      name: test job
      tasks:
        - task_key: main
          notebook_task:
            notebook_path: /Workspace/Users/tester@databricks.com/notebook
