errcode trace $CLI bundle deploy

trace cat "$OUT_REQUESTS" | jq -r 'select(has("path") and .path == "/telemetry-ext") | .body.protoLogs[] | fromjson | .entry.databricks_cli_log.bundle_deploy_event.error_message'

rm "$OUT_REQUESTS"
