bundle:
  name: test-bundle

resources:
  pipelines:
    my_pipeline:
      catalog: main
      target: my_schema
      libraries:
        - notebook:
            path: /Users/{{workspace_user_name}}/notebook
