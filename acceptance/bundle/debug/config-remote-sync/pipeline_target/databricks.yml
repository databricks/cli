bundle:
  name: test-bundle

resources:
  pipelines:
    my_pipeline:
      name: "Pipeline Target Test"
      target: dev_target
      libraries:
        - notebook:
            path: /Users/{{workspace_user_name}}/notebook
