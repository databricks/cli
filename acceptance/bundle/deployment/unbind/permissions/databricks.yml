bundle:
  name: unbind_permissions

resources:
  jobs:
    job_1:
      name: "Job name"
      permissions:
        - level: CAN_MANAGE
          user_name: tester@user.com
        - group_name: jiro-group
          level: CAN_MANAGE
