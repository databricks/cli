Cloud = true

EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["terraform"] # bind,unbind not implemented

[[Repls]]
Old = 'Uploaded \d+ files \(ignored \d+ by .gitignore\)'
New = 'Uploaded [NUMID] files (ignored [NUMID] by .gitignore)'
