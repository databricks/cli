Cloud = false

Ignore = [
  "databricks_empty.yml",
  "databricks_p1.yml",
]

# This test manages DATABRICKS_BUNDLE_ENGINE internally, so set a dummy value
# to prevent inheriting the parent EnvMatrix.
[EnvMatrix]
DATABRICKS_BUNDLE_ENGINE = ["direct"]
