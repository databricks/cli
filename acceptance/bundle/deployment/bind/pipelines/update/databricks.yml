bundle:
  name: test-pipeline-recreate

resources:
  pipelines:
    foo:
      configuration:
        spark.sql.ansi.enabled: "true"
      name: test-pipeline
      libraries:
        - notebook:
            path: ./nb.sql
      catalog: main
