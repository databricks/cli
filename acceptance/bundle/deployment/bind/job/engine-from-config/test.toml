Cloud = false # test leaves deployed job
Ignore = [".databricks"]

# Pin to a single CI runner; the script unsets DATABRICKS_BUNDLE_ENGINE so bind
# must read engine from bundle.engine config.
[EnvMatrix]
  DATABRICKS_BUNDLE_ENGINE = ["direct"]
