Cloud = false # test leaves deployed job
Ignore = [".databricks"]

# Override inherited EnvMatrix to NOT set DATABRICKS_BUNDLE_ENGINE.
# This demonstrates that bind ignores bundle.engine config and existing state:
# deploy reads bundle.engine from config, but bind only reads the env var.
[EnvMatrix]
  DATABRICKS_BUNDLE_ENGINE = []
