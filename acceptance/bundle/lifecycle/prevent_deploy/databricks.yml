bundle:
  name: prevent-deploy

job: &job_base
  resources:
    jobs:
      my_job:
        name: "test-job"
        tasks:
          - task_key: "test-task"
            notebook_task:
              notebook_path: "./test-notebook.py"
        lifecycle:
            prevent_destroy: true

<<: *job_base
