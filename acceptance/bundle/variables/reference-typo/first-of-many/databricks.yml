bundle:
  name: reference-typo-first-of-many

variables:
  host:
    default: https://example.test

resources:
  jobs:
    one:
      # Two typos in one value. Resolution stops at the first unresolved
      # reference, so only "hst" is reported.
      name: ${var.hst}-${var.hostt}
