unset DATABRICKS_HOST
unset DATABRICKS_TOKEN

# Today the commands defined in a script section fail even if the script itself does not require
# authentication. This is because loading a bundle makes the /scim/me call Down the line we could
# make auth optional in the scripts section to match inline script execution behavior.
trace $CLI bundle run abcd
