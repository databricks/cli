bundle:
  name: dms-upload-fails-once

experimental:
  record_deployment_history: true

resources:
  schemas:
    foo:
      name: dms_upload_fails_once_schema
      catalog_name: main
      comment: v1
