# Local only: the failure is injected, and fault.py registers its rule on the fake, which
# a cloud run does not have. Simulating it for real instead - a catalog or principal that
# does not exist - does not work either: the fake accepts both, so the case would stop
# failing locally. Making it portable needs the fake to model that existence.
Cloud = false

