bundle:
  name: dms-failed-update-permissions-$UNIQUE_NAME
experimental:
  deployment_history: true

resources:
  jobs:
    foo:
      name: foo
      permissions:
        - level: CAN_VIEW
          user_name: viewer@example.com
