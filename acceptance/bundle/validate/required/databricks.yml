artifacts:
  zab:
    files:
      - {}

resources:
  models:
    foo:
      description: "hello"

    # Empty string should not trigger a warning.
    bar:
      name: ""

  volumes:
    baz:
      name: "baz"
