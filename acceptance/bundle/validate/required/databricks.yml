bundle:
  name: test-bundle

artifacts:
  my_artifact:
    files:
      - {}

resources:
  jobs:
    my_job_1:
      tasks:
        # job_id being 0 should not trigger a warning.
        - task_key: "task_key1"
          run_job_task:
            job_id: 0

        # job_id not being set should trigger a warning.
        - task_key: "task_key2"
          run_job_task:
