bundle:
  name: test-bundle

definitions:
  cluster1: &cluster1
    num_workers: 1
  cluster2: &cluster2
    spark_version: "13.3.x-scala2.12"

resources:
  jobs:
    my_job:
      name: "test job"
      tasks:
        - task_key: "main"
          new_cluster:
            <<: *cluster1
            <<: *cluster2
          notebook_task:
            notebook_path: "/notebook"
