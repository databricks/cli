bundle:
  name: "model_serving_telemetry"

resources:
  model_serving_endpoints:
    # Telemetry is supported here, but a config naming no profile is discarded.
    endpoint1:
      name: "test-endpoint"
      config:
        served_entities:
          - name: prod
            entity_name: main.default.test_model
            entity_version: "1"
      telemetry_config:
        inference_table_config:
          sampling_fraction: 0.5
