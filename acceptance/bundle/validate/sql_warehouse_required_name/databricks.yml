bundle:
  name: test-bundle

resources:
  sql_warehouses:
    # Missing name: required by the backend, optional in the SDK
    # (json:"name,omitempty").
    my_warehouse:
      cluster_size: "2X-Small"
