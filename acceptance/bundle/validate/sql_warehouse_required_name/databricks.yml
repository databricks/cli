bundle:
  name: test-bundle

resources:
  sql_warehouses:
    # Missing name: required by the backend on create, but modeled as optional
    # (json:"name,omitempty") in the SDK. See DECO-27550.
    my_warehouse:
      cluster_size: "2X-Small"
