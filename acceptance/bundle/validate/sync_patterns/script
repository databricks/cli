trace $CLI bundle validate
trace $CLI bundle validate -o json | jq '.sync'
trace $CLI bundle deploy
jq 'select(.body.multipart_form.path | strings | test("dir/test.yml"))' "$OUT_REQUESTS"  > out.sync.txt
rm "$OUT_REQUESTS"
