bundle:
  name: "model_serving_conversion"

resources:
  model_serving_endpoints:
    endpoint1:
      name: "test-endpoint"
      config:
        served_models:
          - model_name: "my-model"
            model_version: "1"
            scale_to_zero_enabled: true
