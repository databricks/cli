# DBR (Databricks Runtime) specific tests
# These tests run both locally and on DBR with direct deployment engine only.

Local = true
Cloud = true
RunsOnDbr = true

# Only run with direct deployment engine (not terraform)
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct"]

Ignore = [
    ".databricks",
    "databricks.yml",
]
