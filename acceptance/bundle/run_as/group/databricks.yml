bundle:
  name: run_as_group

run_as:
  group_name: bundle_group

resources:
  jobs:
    inherited:
      name: Inherited identity
    user_override:
      name: User override
      run_as:
        user_name: user_override

targets:
  default:
    default: true
  group:
    run_as:
      group_name: target_group
