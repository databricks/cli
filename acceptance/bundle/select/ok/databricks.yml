bundle:
  name: select

resources:
  jobs:
    bar:
      name: job-bar

    foo:
      name: job-foo
      tasks:
        - task_key: run_bar
          run_job_task:
            job_id: ${resources.jobs.bar.id}

    baz:
      name: job-baz
