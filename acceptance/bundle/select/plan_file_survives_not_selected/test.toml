# --select is only supported by the direct engine.
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct"]
# Uses deploy --plan, which is not yet supported with deployment history recording (followup).
EnvMatrix.DMS = [""]
Ignore = [".databricks", ".gitignore", "databricks.yml", "plan.json"]
