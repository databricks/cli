bundle:
  name: plan-select

resources:
  jobs:
    job_a:
      name: job-a
      tasks:
        - task_key: task1
          spark_python_task:
            python_file: "./my_script.py"
          environment_key: "env"
      environments:
        - environment_key: "env"
          spec:
            client: "1"

    job_b:
      name: job-b
      tasks:
        - task_key: task1
          spark_python_task:
            python_file: "./my_script.py"
          environment_key: "env"
      environments:
        - environment_key: "env"
          spec:
            client: "1"

  pipelines:
    my_pipeline:
      name: my-pipeline
