bundle:
  name: test-bundle

resources:
  pipelines:
    my_pipeline:
      catalog: main
      target: default
      configuration:
        key1: value1
      notifications:
        - email_recipients:
            - success@example.com
          alerts:
            - on-update-success
      libraries:
        - notebook:
            path: /Users/{{workspace_user_name}}/notebook
