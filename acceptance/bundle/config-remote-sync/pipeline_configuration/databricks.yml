bundle:
  name: test-bundle

resources:
  pipelines:
    my_pipeline:
      configuration:
        key1: value1
      libraries:
        - notebook:
            path: /Users/{{workspace_user_name}}/notebook
