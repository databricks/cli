Local = true
Cloud = false
RecordRequests = true
# --planmode=offline skips the per-resource remote read, which only the direct engine performs.
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct"]

Ignore = [".databricks", "databricks.yml"]
