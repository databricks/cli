RecordRequests = true

Ignore = [
    '.databricks',
]

# The code archive filename embeds a content hash. Normalize it so output is
# stable across machines while still asserting the <dir>_<hash> shape.
[[Repls]]
Old = 'src_[0-9a-f]{16}\.tar\.gz'
New = 'src_[HASH].tar.gz'
