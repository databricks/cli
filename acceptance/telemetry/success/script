trace $CLI selftest send-telemetry --debug

update_file.py "$OUT_REQUESTS" \
 $(cat "$OUT_REQUESTS" | jq '.headers."User-Agent".[0]'| grep -o 'cmd-exec-id/[^ ]*' | cut -d '/' -f2) \
 '[CMD-EXEC-ID]'

# pretty print the telemetry payload.
trace cat "$OUT_REQUESTS" | jq 'select(has("path") and .path == "/telemetry-ext") | .body.protoLogs[] | fromjson'
