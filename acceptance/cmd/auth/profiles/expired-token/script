sethome "./home"

cat > "./home/.databrickscfg" <<EOF
[expired]
host = ${DATABRICKS_HOST}
token = test-token
EOF

title "Expired token: profile is invalid (NO), full error in valid_reason"
trace $CLI auth profiles --output json
