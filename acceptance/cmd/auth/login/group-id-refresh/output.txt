
>>> [CLI] auth token --profile role --force-refresh
{
  "token_type": "Bearer"
}

>>> jq -s map(select(.path == "/oidc/v1/token")) | length == 1 and (.[0].raw_body | contains("grant_type=refresh_token")) and (.[0].raw_body | contains("assume_group") | not) [TEST_TMP_DIR]/out.requests.txt
true
