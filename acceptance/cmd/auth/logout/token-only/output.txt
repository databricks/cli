
=== Token cache before logout
{
  "tokens": {
    "dev": {
      "access_token": "dev-cached-token",
      "token_type": "Bearer"
    },
    "https://dev.cloud.databricks.com": {
      "access_token": "dev-host-token",
      "token_type": "Bearer"
    }
  },
  "version": 1
}

=== Logout without --delete
>>> [CLI] auth logout --profile dev --force
Successfully logged out of profile "dev". To remove the profile from the config file, use --delete.

=== Config after logout — profile should still exist
[DEFAULT]

[dev]
host = https://dev.cloud.databricks.com
token = dev-token

=== Token cache after logout — both entries should be removed
{
  "tokens": {},
  "version": 1
}
