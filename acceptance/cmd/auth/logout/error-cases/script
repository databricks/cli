sethome "./home"

title "Logout of non-existent profile\n"
musterr $CLI auth logout --profile nonexistent --auto-approve

title "Logout without --profile in non-interactive mode\n"
musterr $CLI auth logout --auto-approve
