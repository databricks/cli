# Workspaces in regions without the sandbox service answer every lakebox
# request with a gateway-level 503. The CLI retries it max503Attempts times,
# then reports the hedged region/availability message.
[[Server]]
Pattern = "GET /api/2.0/lakebox/sandboxes"
Response.StatusCode = 503
Response.Body = 'no healthy upstream'
