# Each run of "Fruit:" lines is sorted before the output is compared, so this test passes
# no matter which order the lines are printed in. Everything else stays put.
echo "Fruit: pear"
echo "Fruit: apple"
echo "Not a fruit: rock"
echo "Fruit: cherry"
echo "Fruit: banana"

# deploy_actions is disabled here, so these keep the order they were printed in.
echo "Created jobs.z"
echo "Created jobs.a"
