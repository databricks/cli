# trace env | sort

trace $CLI current-user me | jq .name

# TODO CONTINUE(v4): I think it works fine for debugging (with the right env vars set in the currnet process).
# does not seem to work for the child process.
trace cat out.requests.txt | jq 'select(has("path") and .path == "/api/2.0/preview/scim/v2/Me")'


# TODO: Cleanup this test.
# rm out.requests.txt
