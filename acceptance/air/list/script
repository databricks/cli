title "list (text)"
trace $CLI air list

title "list (json)"
trace $CLI air list -o json

title "list --all-status (text, via AiTrainingService index)"
trace $CLI air list --all-status

title "list --all-status (json)"
trace $CLI air list --all-status -o json
