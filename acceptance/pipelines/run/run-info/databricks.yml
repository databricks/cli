bundle:
  name: test-pipeline-run

resources:
  pipelines:
    my_pipeline:
      configuration:
        spark.sql.ansi.enabled: "true"
      name: test-pipeline
      libraries:
        - file:
            path: pipeline_file.py
