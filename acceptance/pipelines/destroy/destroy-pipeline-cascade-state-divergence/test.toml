# cascade_on_destroy only affects the direct engine's delete request; the terraform engine
# does not support the attribute yet and rejects it. Restrict this test to direct
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["direct"]
RecordRequests = true
