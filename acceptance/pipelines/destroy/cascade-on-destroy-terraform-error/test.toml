RecordRequests = false

# Static validation only; no need to run on cloud.
Cloud = false

# cascade_on_destroy is direct-only; assert the terraform engine rejects it.
EnvMatrix.DATABRICKS_BUNDLE_ENGINE = ["terraform"]
