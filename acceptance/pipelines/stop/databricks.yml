bundle:
  name: test-pipeline-stop

resources:
  pipelines:
    my_pipeline:
      configuration:
        spark.sql.ansi.enabled: "true"
      name: test-pipeline-stop
      libraries:
        - file:
            path: pipeline_file.py
