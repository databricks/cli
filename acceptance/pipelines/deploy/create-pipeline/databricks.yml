bundle:
  name: test-create-pipeline

resources:
  pipelines:
    foo:
      configuration:
        spark.sql.ansi.enabled: "true"
      name: test-pipeline
      libraries:
        - notebook:
            path: ./nb.sql
