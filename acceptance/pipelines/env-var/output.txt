
=== Test PIPELINES environment variable functionality
>>> echo PIPELINES env var: [PIPELINES]
PIPELINES env var: [PIPELINES]

=== Verify PIPELINES file exists
>>> test -f [PIPELINES]
PIPELINES file exists

=== Verify PIPELINES is executable
>>> test -x [PIPELINES]
PIPELINES is executable

=== Verify PIPELINES binary works like CLI
>>> [PIPELINES] --help
Pipelines CLI

Usage:
  pipelines [command]

Available Commands:
  completion  Generate the autocompletion script for the specified shell
  help        Help about any command
  init        Initialize a new pipelines project

Flags:
      --debug            enable debug logging
  -h, --help             help for pipelines
  -o, --output type      output type: text or json (default text)
  -p, --profile string   ~/.databrickscfg profile
  -t, --target string    project target to use (if applicable)
  -v, --version          version for pipelines

Use "pipelines [command] --help" for more information about a command.
