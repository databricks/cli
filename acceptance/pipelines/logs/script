print_requests() {
    jq --sort-keys 'select((.path | contains("/events")) and (.path | contains("/pipelines")))' < out.requests.txt
    rm out.requests.txt
}

trace $PIPELINES deploy
rm out.requests.txt

title "Calling logs"
trace errcode $PIPELINES logs
print_requests
