title "not deployed yet"
errcode trace $CLI pipelines open

errcode trace $CLI pipelines deploy
replace_ids.py  # normalize resource IDs shown in deploy-summary URLs

title "Modify PATH so that real open is not run"
export PATH=.:$PATH

title "open after deployment. This will fail to open browser and complain, that's ok, we only want the message"
musterr trace $CLI pipelines open

title "open with KEY, expect same output as opening without KEY"
musterr trace $CLI pipelines open test-pipelines-open
