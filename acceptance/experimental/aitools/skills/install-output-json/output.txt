
=== install --output json emits only the JSON document; progress is silenced
>>> [CLI] aitools install --skills-only --scope=global --agents=claude-code --output json
{
  "scope": "global",
  "agents": [
    {
      "name": "claude-code",
      "delivery": "skills",
      "status": "installed"
    }
  ]
}
