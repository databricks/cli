title "private image, but the user cannot create a secret scope to store credentials"
errcode trace DOCKER_CONFIG="$TESTDIR/dockercfg" $CLI experimental air register-image nvcr.io/org/img:1.0

title "same failure as a JSON envelope"
errcode trace DOCKER_CONFIG="$TESTDIR/dockercfg" $CLI experimental air register-image nvcr.io/org/img:1.0 -o json
