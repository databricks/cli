# Code generated by pydabs-codegen. DO NOT EDIT.

from dataclasses import dataclass, field
from typing import TYPE_CHECKING, TypedDict

from databricks.bundles.core._resource import Resource
from databricks.bundles.core._transform import _transform
from databricks.bundles.core._transform_to_json import _transform_to_json_value
from databricks.bundles.core._variable import (
    VariableOr,
    VariableOrList,
    VariableOrOptional,
)
from databricks.bundles.vector_search_indexes._models.delta_sync_vector_index_spec_request import (
    DeltaSyncVectorIndexSpecRequest,
    DeltaSyncVectorIndexSpecRequestParam,
)
from databricks.bundles.vector_search_indexes._models.direct_access_vector_index_spec import (
    DirectAccessVectorIndexSpec,
    DirectAccessVectorIndexSpecParam,
)
from databricks.bundles.vector_search_indexes._models.index_subtype import (
    IndexSubtype,
    IndexSubtypeParam,
)
from databricks.bundles.vector_search_indexes._models.lifecycle import (
    Lifecycle,
    LifecycleParam,
)
from databricks.bundles.vector_search_indexes._models.privilege_assignment import (
    PrivilegeAssignment,
    PrivilegeAssignmentParam,
)
from databricks.bundles.vector_search_indexes._models.vector_index_type import (
    VectorIndexType,
    VectorIndexTypeParam,
)

if TYPE_CHECKING:
    from typing_extensions import Self


@dataclass(kw_only=True)
class VectorSearchIndex(Resource):
    """"""

    endpoint_name: VariableOr[str]
    """
    Name of the endpoint to be used for serving the index
    """

    index_type: VariableOr[VectorIndexType]
    """
    There are 2 types of AI Search indexes:
    - `DELTA_SYNC`: An index that automatically syncs with a source Delta Table, automatically and incrementally updating the index as the underlying data in the Delta Table changes.
    - `DIRECT_ACCESS`: An index that supports direct read and write of vectors and metadata through our REST and SDK APIs. With this model, the user manages index updates.
    """

    name: VariableOr[str]
    """
    Name of the index
    """

    primary_key: VariableOr[str]
    """
    Primary key of the index
    """

    delta_sync_index_spec: VariableOrOptional[DeltaSyncVectorIndexSpecRequest] = None
    """
    Specification for Delta Sync Index. Required if `index_type` is `DELTA_SYNC`.
    """

    direct_access_index_spec: VariableOrOptional[DirectAccessVectorIndexSpec] = None
    """
    Specification for Direct Vector Access Index. Required if `index_type` is `DIRECT_ACCESS`.
    """

    grants: VariableOrList[PrivilegeAssignment] = field(default_factory=list)
    """
    The Unity Catalog privileges to grant to principals on this securable.
    """

    index_subtype: VariableOrOptional[IndexSubtype] = None
    """
    :meta private: [EXPERIMENTAL]
    
    [Beta] The subtype of the index. Use `HYBRID` or `FULL_TEXT`. `VECTOR` is not supported.
    """

    lifecycle: VariableOrOptional[Lifecycle] = None
    """
    Settings that control the deployment lifecycle of the resource, such as preventing it from being destroyed.
    """

    @classmethod
    def from_dict(cls, value: "VectorSearchIndexDict") -> "Self":
        return _transform(cls, value)

    def as_dict(self) -> "VectorSearchIndexDict":
        return _transform_to_json_value(self)  # type:ignore


class VectorSearchIndexDict(TypedDict, total=False):
    """"""

    endpoint_name: VariableOr[str]
    """
    Name of the endpoint to be used for serving the index
    """

    index_type: VariableOr[VectorIndexTypeParam]
    """
    There are 2 types of AI Search indexes:
    - `DELTA_SYNC`: An index that automatically syncs with a source Delta Table, automatically and incrementally updating the index as the underlying data in the Delta Table changes.
    - `DIRECT_ACCESS`: An index that supports direct read and write of vectors and metadata through our REST and SDK APIs. With this model, the user manages index updates.
    """

    name: VariableOr[str]
    """
    Name of the index
    """

    primary_key: VariableOr[str]
    """
    Primary key of the index
    """

    delta_sync_index_spec: VariableOrOptional[DeltaSyncVectorIndexSpecRequestParam]
    """
    Specification for Delta Sync Index. Required if `index_type` is `DELTA_SYNC`.
    """

    direct_access_index_spec: VariableOrOptional[DirectAccessVectorIndexSpecParam]
    """
    Specification for Direct Vector Access Index. Required if `index_type` is `DIRECT_ACCESS`.
    """

    grants: VariableOrList[PrivilegeAssignmentParam]
    """
    The Unity Catalog privileges to grant to principals on this securable.
    """

    index_subtype: VariableOrOptional[IndexSubtypeParam]
    """
    :meta private: [EXPERIMENTAL]
    
    [Beta] The subtype of the index. Use `HYBRID` or `FULL_TEXT`. `VECTOR` is not supported.
    """

    lifecycle: VariableOrOptional[LifecycleParam]
    """
    Settings that control the deployment lifecycle of the resource, such as preventing it from being destroyed.
    """


VectorSearchIndexParam = VectorSearchIndexDict | VectorSearchIndex
