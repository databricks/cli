# Code generated by pydabs-codegen. DO NOT EDIT.

from dataclasses import dataclass, field
from typing import TYPE_CHECKING, TypedDict

from databricks.bundles.core._transform import _transform
from databricks.bundles.core._transform_to_json import _transform_to_json_value
from databricks.bundles.core._variable import VariableOr, VariableOrList

if TYPE_CHECKING:
    from typing_extensions import Self


@dataclass(kw_only=True)
class GoogleAdsCustomReportOptions:
    """
    :meta private: [EXPERIMENTAL]

    User-defined custom report for the Google Ads connector. Mirrors the
    resource + fields + segments + metrics model that Google Ads GAQL exposes.
    The customer account this report runs against is supplied by the source
    schema (namespace), not by this message. The whole message is gated by
    the parent GoogleAdsOptions.custom_report_options stage; per-field stage
    annotations are intentionally omitted.
    Only supported on table-type objects: a custom report requires a
    destination table, so it cannot be specified at the schema/source level.
    """

    resource: VariableOr[str]
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] (Required) Google Ads resource to query (e.g. "ad_group_ad",
    "keyword_view", "search_term_view"). Must be a resource that has
    metrics. Values are validated against Google Ads' field-service
    catalog at pipeline plan time.
    """

    metrics: VariableOrList[str] = field(default_factory=list)
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] (Optional) Metric fields to select (e.g. "metrics.clicks",
    "metrics.cost_micros"). Multiple values are joined into the GAQL
    SELECT clause.
    """

    resource_fields: VariableOrList[str] = field(default_factory=list)
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] (Optional) Resource fields to select, in fully-qualified GAQL form
    (e.g. "ad_group_ad.ad.id", "ad_group_ad.status"). Multiple values are
    joined into the GAQL SELECT clause.
    """

    segments: VariableOrList[str] = field(default_factory=list)
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] (Optional) Segment fields to select (e.g. "segments.date",
    "segments.device"). Must include at least one of segments.date,
    segments.week, or segments.month — that segment is used as the
    incremental cursor for the table.
    """

    @classmethod
    def from_dict(cls, value: "GoogleAdsCustomReportOptionsDict") -> "Self":
        return _transform(cls, value)

    def as_dict(self) -> "GoogleAdsCustomReportOptionsDict":
        return _transform_to_json_value(self)  # type:ignore


class GoogleAdsCustomReportOptionsDict(TypedDict, total=False):
    """"""

    resource: VariableOr[str]
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] (Required) Google Ads resource to query (e.g. "ad_group_ad",
    "keyword_view", "search_term_view"). Must be a resource that has
    metrics. Values are validated against Google Ads' field-service
    catalog at pipeline plan time.
    """

    metrics: VariableOrList[str]
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] (Optional) Metric fields to select (e.g. "metrics.clicks",
    "metrics.cost_micros"). Multiple values are joined into the GAQL
    SELECT clause.
    """

    resource_fields: VariableOrList[str]
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] (Optional) Resource fields to select, in fully-qualified GAQL form
    (e.g. "ad_group_ad.ad.id", "ad_group_ad.status"). Multiple values are
    joined into the GAQL SELECT clause.
    """

    segments: VariableOrList[str]
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] (Optional) Segment fields to select (e.g. "segments.date",
    "segments.device"). Must include at least one of segments.date,
    segments.week, or segments.month — that segment is used as the
    incremental cursor for the table.
    """


GoogleAdsCustomReportOptionsParam = (
    GoogleAdsCustomReportOptionsDict | GoogleAdsCustomReportOptions
)
