# Code generated by pydabs-codegen. DO NOT EDIT.

from collections.abc import Callable
from typing import TYPE_CHECKING, Optional, overload

from databricks.bundles.core._bundle import Bundle
from databricks.bundles.core._location import Location
from databricks.bundles.core._resource_mutator import ResourceMutator
from databricks.bundles.core._transform import _transform

if TYPE_CHECKING:
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.sql_warehouses._models.sql_warehouse import (
        SqlWarehouse,
        SqlWarehouseParam,
    )


def _resource_type() -> "_ResourceType":
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.sql_warehouses._models.sql_warehouse import SqlWarehouse

    return _ResourceType(
        resource_type=SqlWarehouse,
        singular_name="sql_warehouse",
        plural_name="sql_warehouses",
    )


class _SqlWarehouseResources:
    """
    Generated sql_warehouse accessors, mixed into Resources.
    """

    # Provided by the Resources subclass; declared here so the generated methods
    # below type-check.
    _resources: dict[str, dict]

    if TYPE_CHECKING:

        def add_location(self, path: tuple[str, ...], location: Location) -> None: ...

        def add_diagnostic_error(
            self,
            msg: str,
            *,
            detail: Optional[str] = None,
            path: Optional[tuple[str, ...]] = None,
            location: Optional[Location] = None,
        ) -> None: ...

    @property
    def sql_warehouses(self) -> dict[str, "SqlWarehouse"]:
        return self._resources["sql_warehouses"]

    def add_sql_warehouse(
        self,
        resource_name: str,
        sql_warehouse: "SqlWarehouseParam",
        *,
        location: Optional[Location] = None,
    ) -> None:
        """
        Adds the resource sql_warehouse to the collection of resources. Resource name must be unique across all sql_warehouses.

        :param resource_name: unique identifier for the sql_warehouse
        :param sql_warehouse: the sql_warehouse to add, can be SqlWarehouse or dict
        :param location: optional location of the sql_warehouse in the source code
        """
        from databricks.bundles.sql_warehouses._models.sql_warehouse import SqlWarehouse

        sql_warehouse = _transform(SqlWarehouse, sql_warehouse)
        path = ("resources", "sql_warehouses", resource_name)
        location = location or Location.from_stack_frame(depth=1)

        if self._resources["sql_warehouses"].get(resource_name):
            self.add_diagnostic_error(
                msg=f"Duplicate resource name '{resource_name}' for resource 'sql_warehouse'. Resource names must be unique.",
                location=location,
                path=path,
            )
        else:
            if location:
                self.add_location(path, location)

            self._resources["sql_warehouses"][resource_name] = sql_warehouse


@overload
def sql_warehouse_mutator(
    function: Callable[[Bundle, "SqlWarehouse"], "SqlWarehouse"],
) -> ResourceMutator["SqlWarehouse"]: ...


@overload
def sql_warehouse_mutator(
    function: Callable[["SqlWarehouse"], "SqlWarehouse"],
) -> ResourceMutator["SqlWarehouse"]: ...


def sql_warehouse_mutator(function: Callable) -> ResourceMutator["SqlWarehouse"]:
    """
    Decorator for defining mutator for sql_warehouses. Function should return a new instance of the sql_warehouse
    with the desired changes, instead of mutating the input sql_warehouse.

    Example:

    .. code-block:: python

        @sql_warehouse_mutator
        def my_sql_warehouse_mutator(bundle: Bundle, sql_warehouse: SqlWarehouse) -> SqlWarehouse:
            return replace(sql_warehouse, ...)

    :param function: Function that mutates sql_warehouses.
    """
    from databricks.bundles.sql_warehouses._models.sql_warehouse import SqlWarehouse

    return ResourceMutator(resource_type=SqlWarehouse, function=function)
