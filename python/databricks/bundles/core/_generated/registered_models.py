# Code generated by pydabs-codegen. DO NOT EDIT.

from collections.abc import Callable
from typing import TYPE_CHECKING, Optional, overload

from databricks.bundles.core._bundle import Bundle
from databricks.bundles.core._location import Location
from databricks.bundles.core._resource_mutator import ResourceMutator
from databricks.bundles.core._transform import _transform

if TYPE_CHECKING:
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.registered_models._models.registered_model import (
        RegisteredModel,
        RegisteredModelParam,
    )


def _resource_type() -> "_ResourceType":
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.registered_models._models.registered_model import (
        RegisteredModel,
    )

    return _ResourceType(
        resource_type=RegisteredModel,
        singular_name="registered_model",
        plural_name="registered_models",
    )


class _RegisteredModelResources:
    """
    Generated registered_model accessors, mixed into Resources.
    """

    # Provided by the Resources subclass; declared here so the generated methods
    # below type-check.
    _resources: dict[str, dict]

    if TYPE_CHECKING:

        def add_location(self, path: tuple[str, ...], location: Location) -> None: ...

        def add_diagnostic_error(
            self,
            msg: str,
            *,
            detail: Optional[str] = None,
            path: Optional[tuple[str, ...]] = None,
            location: Optional[Location] = None,
        ) -> None: ...

    @property
    def registered_models(self) -> dict[str, "RegisteredModel"]:
        return self._resources["registered_models"]

    def add_registered_model(
        self,
        resource_name: str,
        registered_model: "RegisteredModelParam",
        *,
        location: Optional[Location] = None,
    ) -> None:
        """
        Adds the resource registered_model to the collection of resources. Resource name must be unique across all registered_models.

        :param resource_name: unique identifier for the registered_model
        :param registered_model: the registered_model to add, can be RegisteredModel or dict
        :param location: optional location of the registered_model in the source code
        """
        from databricks.bundles.registered_models._models.registered_model import (
            RegisteredModel,
        )

        registered_model = _transform(RegisteredModel, registered_model)
        path = ("resources", "registered_models", resource_name)
        location = location or Location.from_stack_frame(depth=1)

        if self._resources["registered_models"].get(resource_name):
            self.add_diagnostic_error(
                msg=f"Duplicate resource name '{resource_name}' for resource 'registered_model'. Resource names must be unique.",
                location=location,
                path=path,
            )
        else:
            if location:
                self.add_location(path, location)

            self._resources["registered_models"][resource_name] = registered_model


@overload
def registered_model_mutator(
    function: Callable[[Bundle, "RegisteredModel"], "RegisteredModel"],
) -> ResourceMutator["RegisteredModel"]: ...


@overload
def registered_model_mutator(
    function: Callable[["RegisteredModel"], "RegisteredModel"],
) -> ResourceMutator["RegisteredModel"]: ...


def registered_model_mutator(function: Callable) -> ResourceMutator["RegisteredModel"]:
    """
    Decorator for defining mutator for registered_models. Function should return a new instance of the registered_model
    with the desired changes, instead of mutating the input registered_model.

    Example:

    .. code-block:: python

        @registered_model_mutator
        def my_registered_model_mutator(bundle: Bundle, registered_model: RegisteredModel) -> RegisteredModel:
            return replace(registered_model, ...)

    :param function: Function that mutates registered_models.
    """
    from databricks.bundles.registered_models._models.registered_model import (
        RegisteredModel,
    )

    return ResourceMutator(resource_type=RegisteredModel, function=function)
