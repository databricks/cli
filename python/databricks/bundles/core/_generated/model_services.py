# Code generated by pydabs-codegen. DO NOT EDIT.

from collections.abc import Callable
from typing import TYPE_CHECKING, Optional, overload

from databricks.bundles.core._bundle import Bundle
from databricks.bundles.core._location import Location
from databricks.bundles.core._resource_mutator import ResourceMutator
from databricks.bundles.core._transform import _transform

if TYPE_CHECKING:
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.model_services._models.model_service import (
        ModelService,
        ModelServiceParam,
    )


def _resource_type() -> "_ResourceType":
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.model_services._models.model_service import ModelService

    return _ResourceType(
        resource_type=ModelService,
        singular_name="model_service",
        plural_name="model_services",
    )


class _ModelServiceResources:
    """
    Generated model_service accessors, mixed into Resources.
    """

    # Provided by the Resources subclass; declared here so the generated methods
    # below type-check.
    _resources: dict[str, dict]

    if TYPE_CHECKING:

        def add_location(self, path: tuple[str, ...], location: Location) -> None: ...

        def add_diagnostic_error(
            self,
            msg: str,
            *,
            detail: Optional[str] = None,
            path: Optional[tuple[str, ...]] = None,
            location: Optional[Location] = None,
        ) -> None: ...

    @property
    def model_services(self) -> dict[str, "ModelService"]:
        return self._resources["model_services"]

    def add_model_service(
        self,
        resource_name: str,
        model_service: "ModelServiceParam",
        *,
        location: Optional[Location] = None,
    ) -> None:
        """
        Adds the resource model_service to the collection of resources. Resource name must be unique across all model_services.

        :param resource_name: unique identifier for the model_service
        :param model_service: the model_service to add, can be ModelService or dict
        :param location: optional location of the model_service in the source code
        """
        from databricks.bundles.model_services._models.model_service import ModelService

        model_service = _transform(ModelService, model_service)
        path = ("resources", "model_services", resource_name)
        location = location or Location.from_stack_frame(depth=1)

        if self._resources["model_services"].get(resource_name):
            self.add_diagnostic_error(
                msg=f"Duplicate resource name '{resource_name}' for resource 'model_service'. Resource names must be unique.",
                location=location,
                path=path,
            )
        else:
            if location:
                self.add_location(path, location)

            self._resources["model_services"][resource_name] = model_service


@overload
def model_service_mutator(
    function: Callable[[Bundle, "ModelService"], "ModelService"],
) -> ResourceMutator["ModelService"]: ...


@overload
def model_service_mutator(
    function: Callable[["ModelService"], "ModelService"],
) -> ResourceMutator["ModelService"]: ...


def model_service_mutator(function: Callable) -> ResourceMutator["ModelService"]:
    """
    Decorator for defining mutator for model_services. Function should return a new instance of the model_service
    with the desired changes, instead of mutating the input model_service.

    Example:

    .. code-block:: python

        @model_service_mutator
        def my_model_service_mutator(bundle: Bundle, model_service: ModelService) -> ModelService:
            return replace(model_service, ...)

    :param function: Function that mutates model_services.
    """
    from databricks.bundles.model_services._models.model_service import ModelService

    return ResourceMutator(resource_type=ModelService, function=function)
