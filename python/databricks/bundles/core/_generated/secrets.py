# Code generated by pydabs-codegen. DO NOT EDIT.

from collections.abc import Callable
from typing import TYPE_CHECKING, Optional, overload

from databricks.bundles.core._bundle import Bundle
from databricks.bundles.core._location import Location
from databricks.bundles.core._resource_mutator import ResourceMutator
from databricks.bundles.core._transform import _transform

if TYPE_CHECKING:
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.secrets._models.secret import Secret, SecretParam


def _resource_type() -> "_ResourceType":
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.secrets._models.secret import Secret

    return _ResourceType(
        resource_type=Secret,
        singular_name="secret",
        plural_name="secrets",
    )


class _SecretResources:
    """
    Generated secret accessors, mixed into Resources.
    """

    # Provided by the Resources subclass; declared here so the generated methods
    # below type-check.
    _resources: dict[str, dict]

    if TYPE_CHECKING:

        def add_location(self, path: tuple[str, ...], location: Location) -> None: ...

        def add_diagnostic_error(
            self,
            msg: str,
            *,
            detail: Optional[str] = None,
            path: Optional[tuple[str, ...]] = None,
            location: Optional[Location] = None,
        ) -> None: ...

    @property
    def secrets(self) -> dict[str, "Secret"]:
        return self._resources["secrets"]

    def add_secret(
        self,
        resource_name: str,
        secret: "SecretParam",
        *,
        location: Optional[Location] = None,
    ) -> None:
        """
        Adds the resource secret to the collection of resources. Resource name must be unique across all secrets.

        :param resource_name: unique identifier for the secret
        :param secret: the secret to add, can be Secret or dict
        :param location: optional location of the secret in the source code
        """
        from databricks.bundles.secrets._models.secret import Secret

        secret = _transform(Secret, secret)
        path = ("resources", "secrets", resource_name)
        location = location or Location.from_stack_frame(depth=1)

        if self._resources["secrets"].get(resource_name):
            self.add_diagnostic_error(
                msg=f"Duplicate resource name '{resource_name}' for resource 'secret'. Resource names must be unique.",
                location=location,
                path=path,
            )
        else:
            if location:
                self.add_location(path, location)

            self._resources["secrets"][resource_name] = secret


@overload
def secret_mutator(
    function: Callable[[Bundle, "Secret"], "Secret"],
) -> ResourceMutator["Secret"]: ...


@overload
def secret_mutator(
    function: Callable[["Secret"], "Secret"],
) -> ResourceMutator["Secret"]: ...


def secret_mutator(function: Callable) -> ResourceMutator["Secret"]:
    """
    Decorator for defining mutator for secrets. Function should return a new instance of the secret
    with the desired changes, instead of mutating the input secret.

    Example:

    .. code-block:: python

        @secret_mutator
        def my_secret_mutator(bundle: Bundle, secret: Secret) -> Secret:
            return replace(secret, ...)

    :param function: Function that mutates secrets.
    """
    from databricks.bundles.secrets._models.secret import Secret

    return ResourceMutator(resource_type=Secret, function=function)
