# Code generated by pydabs-codegen. DO NOT EDIT.

from collections.abc import Callable
from typing import TYPE_CHECKING, Optional, overload

from databricks.bundles.core._bundle import Bundle
from databricks.bundles.core._location import Location
from databricks.bundles.core._resource_mutator import ResourceMutator
from databricks.bundles.core._transform import _transform

if TYPE_CHECKING:
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.dashboards._models.dashboard import (
        Dashboard,
        DashboardParam,
    )


def _resource_type() -> "_ResourceType":
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.dashboards._models.dashboard import Dashboard

    return _ResourceType(
        resource_type=Dashboard,
        singular_name="dashboard",
        plural_name="dashboards",
    )


class _DashboardResources:
    """
    Generated dashboard accessors, mixed into Resources.
    """

    # Provided by the Resources subclass; declared here so the generated methods
    # below type-check.
    _resources: dict[str, dict]

    if TYPE_CHECKING:

        def add_location(self, path: tuple[str, ...], location: Location) -> None: ...

        def add_diagnostic_error(
            self,
            msg: str,
            *,
            detail: Optional[str] = None,
            path: Optional[tuple[str, ...]] = None,
            location: Optional[Location] = None,
        ) -> None: ...

    @property
    def dashboards(self) -> dict[str, "Dashboard"]:
        return self._resources["dashboards"]

    def add_dashboard(
        self,
        resource_name: str,
        dashboard: "DashboardParam",
        *,
        location: Optional[Location] = None,
    ) -> None:
        """
        Adds the resource dashboard to the collection of resources. Resource name must be unique across all dashboards.

        :param resource_name: unique identifier for the dashboard
        :param dashboard: the dashboard to add, can be Dashboard or dict
        :param location: optional location of the dashboard in the source code
        """
        from databricks.bundles.dashboards._models.dashboard import Dashboard

        dashboard = _transform(Dashboard, dashboard)
        path = ("resources", "dashboards", resource_name)
        location = location or Location.from_stack_frame(depth=1)

        if self._resources["dashboards"].get(resource_name):
            self.add_diagnostic_error(
                msg=f"Duplicate resource name '{resource_name}' for resource 'dashboard'. Resource names must be unique.",
                location=location,
                path=path,
            )
        else:
            if location:
                self.add_location(path, location)

            self._resources["dashboards"][resource_name] = dashboard


@overload
def dashboard_mutator(
    function: Callable[[Bundle, "Dashboard"], "Dashboard"],
) -> ResourceMutator["Dashboard"]: ...


@overload
def dashboard_mutator(
    function: Callable[["Dashboard"], "Dashboard"],
) -> ResourceMutator["Dashboard"]: ...


def dashboard_mutator(function: Callable) -> ResourceMutator["Dashboard"]:
    """
    Decorator for defining mutator for dashboards. Function should return a new instance of the dashboard
    with the desired changes, instead of mutating the input dashboard.

    Example:

    .. code-block:: python

        @dashboard_mutator
        def my_dashboard_mutator(bundle: Bundle, dashboard: Dashboard) -> Dashboard:
            return replace(dashboard, ...)

    :param function: Function that mutates dashboards.
    """
    from databricks.bundles.dashboards._models.dashboard import Dashboard

    return ResourceMutator(resource_type=Dashboard, function=function)
