# Code generated by pydabs-codegen. DO NOT EDIT.

from collections.abc import Callable
from typing import TYPE_CHECKING, Optional, overload

from databricks.bundles.core._bundle import Bundle
from databricks.bundles.core._location import Location
from databricks.bundles.core._resource_mutator import ResourceMutator
from databricks.bundles.core._transform import _transform

if TYPE_CHECKING:
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.models._models.mlflow_model import (
        MlflowModel,
        MlflowModelParam,
    )


def _resource_type() -> "_ResourceType":
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.models._models.mlflow_model import MlflowModel

    return _ResourceType(
        resource_type=MlflowModel,
        singular_name="mlflow_model",
        plural_name="models",
    )


class _MlflowModelResources:
    """
    Generated mlflow_model accessors, mixed into Resources.
    """

    # Provided by the Resources subclass; declared here so the generated methods
    # below type-check.
    _resources: dict[str, dict]

    if TYPE_CHECKING:

        def add_location(self, path: tuple[str, ...], location: Location) -> None: ...

        def add_diagnostic_error(
            self,
            msg: str,
            *,
            detail: Optional[str] = None,
            path: Optional[tuple[str, ...]] = None,
            location: Optional[Location] = None,
        ) -> None: ...

    @property
    def models(self) -> dict[str, "MlflowModel"]:
        return self._resources["models"]

    def add_mlflow_model(
        self,
        resource_name: str,
        mlflow_model: "MlflowModelParam",
        *,
        location: Optional[Location] = None,
    ) -> None:
        """
        Adds the resource mlflow_model to the collection of resources. Resource name must be unique across all models.

        :param resource_name: unique identifier for the mlflow_model
        :param mlflow_model: the mlflow_model to add, can be MlflowModel or dict
        :param location: optional location of the mlflow_model in the source code
        """
        from databricks.bundles.models._models.mlflow_model import MlflowModel

        mlflow_model = _transform(MlflowModel, mlflow_model)
        path = ("resources", "models", resource_name)
        location = location or Location.from_stack_frame(depth=1)

        if self._resources["models"].get(resource_name):
            self.add_diagnostic_error(
                msg=f"Duplicate resource name '{resource_name}' for resource 'mlflow_model'. Resource names must be unique.",
                location=location,
                path=path,
            )
        else:
            if location:
                self.add_location(path, location)

            self._resources["models"][resource_name] = mlflow_model


@overload
def mlflow_model_mutator(
    function: Callable[[Bundle, "MlflowModel"], "MlflowModel"],
) -> ResourceMutator["MlflowModel"]: ...


@overload
def mlflow_model_mutator(
    function: Callable[["MlflowModel"], "MlflowModel"],
) -> ResourceMutator["MlflowModel"]: ...


def mlflow_model_mutator(function: Callable) -> ResourceMutator["MlflowModel"]:
    """
    Decorator for defining mutator for models. Function should return a new instance of the mlflow_model
    with the desired changes, instead of mutating the input mlflow_model.

    Example:

    .. code-block:: python

        @mlflow_model_mutator
        def my_mlflow_model_mutator(bundle: Bundle, mlflow_model: MlflowModel) -> MlflowModel:
            return replace(mlflow_model, ...)

    :param function: Function that mutates models.
    """
    from databricks.bundles.models._models.mlflow_model import MlflowModel

    return ResourceMutator(resource_type=MlflowModel, function=function)
