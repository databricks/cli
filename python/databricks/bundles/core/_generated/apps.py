# Code generated by pydabs-codegen. DO NOT EDIT.

from collections.abc import Callable
from typing import TYPE_CHECKING, Optional, overload

from databricks.bundles.core._bundle import Bundle
from databricks.bundles.core._location import Location
from databricks.bundles.core._resource_mutator import ResourceMutator
from databricks.bundles.core._transform import _transform

if TYPE_CHECKING:
    from databricks.bundles.apps._models.app import App, AppParam
    from databricks.bundles.core._resource_type import _ResourceType


def _resource_type() -> "_ResourceType":
    from databricks.bundles.apps._models.app import App
    from databricks.bundles.core._resource_type import _ResourceType

    return _ResourceType(
        resource_type=App,
        singular_name="app",
        plural_name="apps",
    )


class _AppResources:
    """
    Generated app accessors, mixed into Resources.
    """

    # Provided by the Resources subclass; declared here so the generated methods
    # below type-check.
    _resources: dict[str, dict]

    if TYPE_CHECKING:

        def add_location(self, path: tuple[str, ...], location: Location) -> None: ...

        def add_diagnostic_error(
            self,
            msg: str,
            *,
            detail: Optional[str] = None,
            path: Optional[tuple[str, ...]] = None,
            location: Optional[Location] = None,
        ) -> None: ...

    @property
    def apps(self) -> dict[str, "App"]:
        return self._resources["apps"]

    def add_app(
        self,
        resource_name: str,
        app: "AppParam",
        *,
        location: Optional[Location] = None,
    ) -> None:
        """
        Adds the resource app to the collection of resources. Resource name must be unique across all apps.

        :param resource_name: unique identifier for the app
        :param app: the app to add, can be App or dict
        :param location: optional location of the app in the source code
        """
        from databricks.bundles.apps._models.app import App

        app = _transform(App, app)
        path = ("resources", "apps", resource_name)
        location = location or Location.from_stack_frame(depth=1)

        if self._resources["apps"].get(resource_name):
            self.add_diagnostic_error(
                msg=f"Duplicate resource name '{resource_name}' for resource 'app'. Resource names must be unique.",
                location=location,
                path=path,
            )
        else:
            if location:
                self.add_location(path, location)

            self._resources["apps"][resource_name] = app


@overload
def app_mutator(
    function: Callable[[Bundle, "App"], "App"],
) -> ResourceMutator["App"]: ...


@overload
def app_mutator(
    function: Callable[["App"], "App"],
) -> ResourceMutator["App"]: ...


def app_mutator(function: Callable) -> ResourceMutator["App"]:
    """
    Decorator for defining mutator for apps. Function should return a new instance of the app
    with the desired changes, instead of mutating the input app.

    Example:

    .. code-block:: python

        @app_mutator
        def my_app_mutator(bundle: Bundle, app: App) -> App:
            return replace(app, ...)

    :param function: Function that mutates apps.
    """
    from databricks.bundles.apps._models.app import App

    return ResourceMutator(resource_type=App, function=function)
