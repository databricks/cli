# Code generated by pydabs-codegen. DO NOT EDIT.

from collections.abc import Callable
from typing import TYPE_CHECKING, Optional, overload

from databricks.bundles.core._bundle import Bundle
from databricks.bundles.core._location import Location
from databricks.bundles.core._resource_mutator import ResourceMutator
from databricks.bundles.core._transform import _transform

if TYPE_CHECKING:
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.model_provider_services._models.model_provider_service import (
        ModelProviderService,
        ModelProviderServiceParam,
    )


def _resource_type() -> "_ResourceType":
    from databricks.bundles.core._resource_type import _ResourceType
    from databricks.bundles.model_provider_services._models.model_provider_service import (
        ModelProviderService,
    )

    return _ResourceType(
        resource_type=ModelProviderService,
        singular_name="model_provider_service",
        plural_name="model_provider_services",
    )


class _ModelProviderServiceResources:
    """
    Generated model_provider_service accessors, mixed into Resources.
    """

    # Provided by the Resources subclass; declared here so the generated methods
    # below type-check.
    _resources: dict[str, dict]

    if TYPE_CHECKING:

        def add_location(self, path: tuple[str, ...], location: Location) -> None: ...

        def add_diagnostic_error(
            self,
            msg: str,
            *,
            detail: Optional[str] = None,
            path: Optional[tuple[str, ...]] = None,
            location: Optional[Location] = None,
        ) -> None: ...

    @property
    def model_provider_services(self) -> dict[str, "ModelProviderService"]:
        return self._resources["model_provider_services"]

    def add_model_provider_service(
        self,
        resource_name: str,
        model_provider_service: "ModelProviderServiceParam",
        *,
        location: Optional[Location] = None,
    ) -> None:
        """
        Adds the resource model_provider_service to the collection of resources. Resource name must be unique across all model_provider_services.

        :param resource_name: unique identifier for the model_provider_service
        :param model_provider_service: the model_provider_service to add, can be ModelProviderService or dict
        :param location: optional location of the model_provider_service in the source code
        """
        from databricks.bundles.model_provider_services._models.model_provider_service import (
            ModelProviderService,
        )

        model_provider_service = _transform(
            ModelProviderService, model_provider_service
        )
        path = ("resources", "model_provider_services", resource_name)
        location = location or Location.from_stack_frame(depth=1)

        if self._resources["model_provider_services"].get(resource_name):
            self.add_diagnostic_error(
                msg=f"Duplicate resource name '{resource_name}' for resource 'model_provider_service'. Resource names must be unique.",
                location=location,
                path=path,
            )
        else:
            if location:
                self.add_location(path, location)

            self._resources["model_provider_services"][resource_name] = (
                model_provider_service
            )


@overload
def model_provider_service_mutator(
    function: Callable[[Bundle, "ModelProviderService"], "ModelProviderService"],
) -> ResourceMutator["ModelProviderService"]: ...


@overload
def model_provider_service_mutator(
    function: Callable[["ModelProviderService"], "ModelProviderService"],
) -> ResourceMutator["ModelProviderService"]: ...


def model_provider_service_mutator(
    function: Callable,
) -> ResourceMutator["ModelProviderService"]:
    """
    Decorator for defining mutator for model_provider_services. Function should return a new instance of the model_provider_service
    with the desired changes, instead of mutating the input model_provider_service.

    Example:

    .. code-block:: python

        @model_provider_service_mutator
        def my_model_provider_service_mutator(bundle: Bundle, model_provider_service: ModelProviderService) -> ModelProviderService:
            return replace(model_provider_service, ...)

    :param function: Function that mutates model_provider_services.
    """
    from databricks.bundles.model_provider_services._models.model_provider_service import (
        ModelProviderService,
    )

    return ResourceMutator(resource_type=ModelProviderService, function=function)
