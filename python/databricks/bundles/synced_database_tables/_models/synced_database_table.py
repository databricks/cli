# Code generated by pydabs-codegen. DO NOT EDIT.

from dataclasses import dataclass
from typing import TYPE_CHECKING, TypedDict

from databricks.bundles.core._resource import Resource
from databricks.bundles.core._transform import _transform
from databricks.bundles.core._transform_to_json import _transform_to_json_value
from databricks.bundles.core._variable import VariableOr, VariableOrOptional
from databricks.bundles.synced_database_tables._models.lifecycle import (
    Lifecycle,
    LifecycleParam,
)
from databricks.bundles.synced_database_tables._models.synced_table_spec import (
    SyncedTableSpec,
    SyncedTableSpecParam,
)

if TYPE_CHECKING:
    from typing_extensions import Self


@dataclass(kw_only=True)
class SyncedDatabaseTable(Resource):
    """"""

    name: VariableOr[str]
    """
    [Public Preview] Full three-part (catalog, schema, table) name of the table.
    """

    database_instance_name: VariableOrOptional[str] = None
    """
    [Public Preview] Name of the target database instance. This is required when creating synced database tables in standard catalogs.
    This is optional when creating synced database tables in registered catalogs. If this field is specified
    when creating synced database tables in registered catalogs, the database instance name MUST
    match that of the registered catalog (or the request will be rejected).
    """

    lifecycle: VariableOrOptional[Lifecycle] = None
    """
    Settings that control the deployment lifecycle of the resource, such as preventing it from being destroyed.
    """

    logical_database_name: VariableOrOptional[str] = None
    """
    [Public Preview] Target Postgres database object (logical database) name for this table.
    
    When creating a synced table in a registered Postgres catalog, the
    target Postgres database name is inferred to be that of the registered catalog.
    If this field is specified in this scenario, the Postgres database name MUST
    match that of the registered catalog (or the request will be rejected).
    
    When creating a synced table in a standard catalog, this field is required.
    In this scenario, specifying this field will allow targeting an arbitrary postgres database.
    Note that this has implications for the `create_database_objects_is_missing` field in `spec`.
    """

    spec: VariableOrOptional[SyncedTableSpec] = None
    """
    [Public Preview] Specification of a synced database table.
    """

    @classmethod
    def from_dict(cls, value: "SyncedDatabaseTableDict") -> "Self":
        return _transform(cls, value)

    def as_dict(self) -> "SyncedDatabaseTableDict":
        return _transform_to_json_value(self)  # type:ignore


class SyncedDatabaseTableDict(TypedDict, total=False):
    """"""

    name: VariableOr[str]
    """
    [Public Preview] Full three-part (catalog, schema, table) name of the table.
    """

    database_instance_name: VariableOrOptional[str]
    """
    [Public Preview] Name of the target database instance. This is required when creating synced database tables in standard catalogs.
    This is optional when creating synced database tables in registered catalogs. If this field is specified
    when creating synced database tables in registered catalogs, the database instance name MUST
    match that of the registered catalog (or the request will be rejected).
    """

    lifecycle: VariableOrOptional[LifecycleParam]
    """
    Settings that control the deployment lifecycle of the resource, such as preventing it from being destroyed.
    """

    logical_database_name: VariableOrOptional[str]
    """
    [Public Preview] Target Postgres database object (logical database) name for this table.
    
    When creating a synced table in a registered Postgres catalog, the
    target Postgres database name is inferred to be that of the registered catalog.
    If this field is specified in this scenario, the Postgres database name MUST
    match that of the registered catalog (or the request will be rejected).
    
    When creating a synced table in a standard catalog, this field is required.
    In this scenario, specifying this field will allow targeting an arbitrary postgres database.
    Note that this has implications for the `create_database_objects_is_missing` field in `spec`.
    """

    spec: VariableOrOptional[SyncedTableSpecParam]
    """
    [Public Preview] Specification of a synced database table.
    """


SyncedDatabaseTableParam = SyncedDatabaseTableDict | SyncedDatabaseTable
