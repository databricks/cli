# Code generated by pydabs-codegen. DO NOT EDIT.

from dataclasses import dataclass, field
from typing import TYPE_CHECKING, TypedDict

from databricks.bundles.core._resource import Resource
from databricks.bundles.core._transform import _transform
from databricks.bundles.core._transform_to_json import _transform_to_json_value
from databricks.bundles.core._variable import (
    VariableOr,
    VariableOrList,
    VariableOrOptional,
)
from databricks.bundles.experiments._models.experiment_tag import (
    ExperimentTag,
    ExperimentTagParam,
)
from databricks.bundles.experiments._models.experiment_trace_location import (
    ExperimentTraceLocation,
    ExperimentTraceLocationParam,
)
from databricks.bundles.experiments._models.lifecycle import (
    Lifecycle,
    LifecycleParam,
)
from databricks.bundles.experiments._models.mlflow_experiment_permission import (
    MlflowExperimentPermission,
    MlflowExperimentPermissionParam,
)

if TYPE_CHECKING:
    from typing_extensions import Self


@dataclass(kw_only=True)
class MlflowExperiment(Resource):
    """"""

    name: VariableOr[str]
    """
    Experiment name.
    """

    artifact_location: VariableOrOptional[str] = None
    """
    Location where all artifacts for the experiment are stored.
    If not provided, the remote server will select an appropriate default.
    """

    lifecycle: VariableOrOptional[Lifecycle] = None
    """
    Settings that control the deployment lifecycle of the resource, such as preventing it from being destroyed.
    """

    permissions: VariableOrList[MlflowExperimentPermission] = field(
        default_factory=list
    )
    """
    The permissions to apply to this resource.
    """

    tags: VariableOrList[ExperimentTag] = field(default_factory=list)
    """
    A collection of tags to set on the experiment. Maximum tag size and number of tags per request
    depends on the storage backend. All storage backends are guaranteed to support tag keys up
    to 250 bytes in size and tag values up to 5000 bytes in size. All storage backends are also
    guaranteed to support up to 20 tags per request.
    """

    trace_location: VariableOrOptional[ExperimentTraceLocation] = None
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] The location where the experiment's traces are stored. When set, the
    underlying storage is provisioned and the experiment's traces are routed
    to it. When unset, traces are stored in the default MLflow backend. This
    field cannot be updated after the experiment is created.
    """

    @classmethod
    def from_dict(cls, value: "MlflowExperimentDict") -> "Self":
        return _transform(cls, value)

    def as_dict(self) -> "MlflowExperimentDict":
        return _transform_to_json_value(self)  # type:ignore


class MlflowExperimentDict(TypedDict, total=False):
    """"""

    name: VariableOr[str]
    """
    Experiment name.
    """

    artifact_location: VariableOrOptional[str]
    """
    Location where all artifacts for the experiment are stored.
    If not provided, the remote server will select an appropriate default.
    """

    lifecycle: VariableOrOptional[LifecycleParam]
    """
    Settings that control the deployment lifecycle of the resource, such as preventing it from being destroyed.
    """

    permissions: VariableOrList[MlflowExperimentPermissionParam]
    """
    The permissions to apply to this resource.
    """

    tags: VariableOrList[ExperimentTagParam]
    """
    A collection of tags to set on the experiment. Maximum tag size and number of tags per request
    depends on the storage backend. All storage backends are guaranteed to support tag keys up
    to 250 bytes in size and tag values up to 5000 bytes in size. All storage backends are also
    guaranteed to support up to 20 tags per request.
    """

    trace_location: VariableOrOptional[ExperimentTraceLocationParam]
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] The location where the experiment's traces are stored. When set, the
    underlying storage is provisioned and the experiment's traces are routed
    to it. When unset, traces are stored in the default MLflow backend. This
    field cannot be updated after the experiment is created.
    """


MlflowExperimentParam = MlflowExperimentDict | MlflowExperiment
