# Code generated by pydabs-codegen. DO NOT EDIT.

from dataclasses import dataclass
from typing import TYPE_CHECKING, TypedDict

from databricks.bundles.core._transform import _transform
from databricks.bundles.core._transform_to_json import _transform_to_json_value
from databricks.bundles.core._variable import VariableOr
from databricks.bundles.model_services._models.model_provider_service_config_model_target_config import (
    ModelProviderServiceConfigModelTargetConfig,
    ModelProviderServiceConfigModelTargetConfigParam,
)

if TYPE_CHECKING:
    from typing_extensions import Self


@dataclass(kw_only=True)
class ModelServiceConfigExternalModelConfig:
    """
    Configuration for an external-foundation-model destination. Provider auth
    and provider-specific cloud configuration are owned by a separate, governed
    ModelProviderService entity referenced via `model_provider_service`; the
    platform resolves the provider at invocation time.
    """

    model_provider_service: VariableOr[str]
    """
    :meta private: [EXPERIMENTAL]
    
    [Beta] Resource name of the governed ModelProviderService that owns provider
    auth and provider-specific configuration. The referenced
    ModelProviderService also carries the provider type, so this message
    does not surface it directly.
    Format: `model-provider-services/{catalog}.{schema}.{model_provider_service}`.
    Each `{...}` component is capped at 255 characters individually.
    """

    target: VariableOr[ModelProviderServiceConfigModelTargetConfig]
    """
    :meta private: [EXPERIMENTAL]
    
    [Beta] Routing target for the destination: the provider-side model selected from
    the referenced ModelProviderService's `targets` catalog, plus the unified
    API types the platform should translate to/from at request time.
    """

    @classmethod
    def from_dict(cls, value: "ModelServiceConfigExternalModelConfigDict") -> "Self":
        return _transform(cls, value)

    def as_dict(self) -> "ModelServiceConfigExternalModelConfigDict":
        return _transform_to_json_value(self)  # type:ignore


class ModelServiceConfigExternalModelConfigDict(TypedDict, total=False):
    """"""

    model_provider_service: VariableOr[str]
    """
    :meta private: [EXPERIMENTAL]
    
    [Beta] Resource name of the governed ModelProviderService that owns provider
    auth and provider-specific configuration. The referenced
    ModelProviderService also carries the provider type, so this message
    does not surface it directly.
    Format: `model-provider-services/{catalog}.{schema}.{model_provider_service}`.
    Each `{...}` component is capped at 255 characters individually.
    """

    target: VariableOr[ModelProviderServiceConfigModelTargetConfigParam]
    """
    :meta private: [EXPERIMENTAL]
    
    [Beta] Routing target for the destination: the provider-side model selected from
    the referenced ModelProviderService's `targets` catalog, plus the unified
    API types the platform should translate to/from at request time.
    """


ModelServiceConfigExternalModelConfigParam = (
    ModelServiceConfigExternalModelConfigDict | ModelServiceConfigExternalModelConfig
)
