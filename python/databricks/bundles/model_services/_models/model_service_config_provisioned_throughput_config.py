# Code generated by pydabs-codegen. DO NOT EDIT.

from dataclasses import dataclass
from typing import TYPE_CHECKING, TypedDict

from databricks.bundles.core._transform import _transform
from databricks.bundles.core._transform_to_json import _transform_to_json_value
from databricks.bundles.core._variable import VariableOr

if TYPE_CHECKING:
    from typing_extensions import Self


@dataclass(kw_only=True)
class ModelServiceConfigProvisionedThroughputConfig:
    """
    Configuration for a provisioned-throughput foundation-model destination.
    References a pre-existing Model Serving endpoint that serves the model;
    sizing (provisioned throughput, burst scaling, model version) is owned by
    the Model Serving endpoint itself, not by this message.
    """

    model_serving_endpoint: VariableOr[str]
    """
    :meta private: [EXPERIMENTAL]
    
    [Beta] Name of the backing Model Serving endpoint serving the provisioned-
    throughput foundation model, as the AIP-122 typed resource name
    `serving-endpoints/{name}`. The same UC model can be served on multiple
    Model Serving endpoints (different throughput / region / config); the
    caller picks which one this destination routes to. The endpoint must
    exist at create time.
    """

    @classmethod
    def from_dict(
        cls, value: "ModelServiceConfigProvisionedThroughputConfigDict"
    ) -> "Self":
        return _transform(cls, value)

    def as_dict(self) -> "ModelServiceConfigProvisionedThroughputConfigDict":
        return _transform_to_json_value(self)  # type:ignore


class ModelServiceConfigProvisionedThroughputConfigDict(TypedDict, total=False):
    """"""

    model_serving_endpoint: VariableOr[str]
    """
    :meta private: [EXPERIMENTAL]
    
    [Beta] Name of the backing Model Serving endpoint serving the provisioned-
    throughput foundation model, as the AIP-122 typed resource name
    `serving-endpoints/{name}`. The same UC model can be served on multiple
    Model Serving endpoints (different throughput / region / config); the
    caller picks which one this destination routes to. The endpoint must
    exist at create time.
    """


ModelServiceConfigProvisionedThroughputConfigParam = (
    ModelServiceConfigProvisionedThroughputConfigDict
    | ModelServiceConfigProvisionedThroughputConfig
)
