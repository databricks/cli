# Code generated by pydabs-codegen. DO NOT EDIT.

from dataclasses import dataclass
from typing import TYPE_CHECKING, TypedDict

from databricks.bundles.core._transform import _transform
from databricks.bundles.core._transform_to_json import _transform_to_json_value
from databricks.bundles.core._variable import VariableOr, VariableOrOptional
from databricks.bundles.postgres_snapshot_schedules._models.daily_schedule import (
    DailySchedule,
    DailyScheduleParam,
)
from databricks.bundles.postgres_snapshot_schedules._models.monthly_schedule import (
    MonthlySchedule,
    MonthlyScheduleParam,
)
from databricks.bundles.postgres_snapshot_schedules._models.weekly_schedule import (
    WeeklySchedule,
    WeeklyScheduleParam,
)

if TYPE_CHECKING:
    from typing_extensions import Self


@dataclass(kw_only=True)
class ScheduleCadence:
    """
    One cadence at which automatic snapshots are taken.
    """

    retention: VariableOr[str]
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] How long snapshots from this cadence are kept before automatic deletion.
    Must be at least 1 hour. Applied when a snapshot is taken; not retroactive,
    so changing it affects only later snapshots.
    """

    daily_schedule: VariableOrOptional[DailySchedule] = None
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] Take a snapshot once per day.
    """

    monthly_schedule: VariableOrOptional[MonthlySchedule] = None
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] Take a snapshot once per month.
    """

    weekly_schedule: VariableOrOptional[WeeklySchedule] = None
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] Take a snapshot once per week.
    """

    @classmethod
    def from_dict(cls, value: "ScheduleCadenceDict") -> "Self":
        return _transform(cls, value)

    def as_dict(self) -> "ScheduleCadenceDict":
        return _transform_to_json_value(self)  # type:ignore


class ScheduleCadenceDict(TypedDict, total=False):
    """"""

    retention: VariableOr[str]
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] How long snapshots from this cadence are kept before automatic deletion.
    Must be at least 1 hour. Applied when a snapshot is taken; not retroactive,
    so changing it affects only later snapshots.
    """

    daily_schedule: VariableOrOptional[DailyScheduleParam]
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] Take a snapshot once per day.
    """

    monthly_schedule: VariableOrOptional[MonthlyScheduleParam]
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] Take a snapshot once per month.
    """

    weekly_schedule: VariableOrOptional[WeeklyScheduleParam]
    """
    :meta private: [EXPERIMENTAL]
    
    [Private Preview] Take a snapshot once per week.
    """


ScheduleCadenceParam = ScheduleCadenceDict | ScheduleCadence
