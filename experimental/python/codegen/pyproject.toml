[project]
name = "codegen"
version = "0.1.0"
description = "Code generator for Python"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
addopts = "-ra -q -vv"
testpaths = [
    "databricks_tests",
]
