{{- /*
     * Template for the AGENTS.md / CLAUDE.md file created in new projects.
     *
     * Goal for this file is to point agents to the core Databricks skill for guidance.
     * A secondary goal is to offer a placeholder to users to extend with their own instructions.
     * It is a non-goal to inline all instructions.
     *
     * Note the app template currently has its own AGENTS.md / CLAUDE.md file.
     *
     */ -}}

# Databricks Asset Bundles Project

This project uses Databricks Asset Bundles for deployment. Add project-specific instructions below.

## For AI Agents: MANDATORY First Action

**BEFORE any other action, read the `databricks` skill (`databricks/SKILL.md`).**

The skill provides:
- CLI installation and authentication guidance
- Profile selection rules
- Data discovery commands
- Bundle deployment workflow
- Links to product-specific skills (apps, jobs, pipelines)

If the skill is not available, the user needs to install Databricks agent skills:

```bash
databricks experimental aitools skills install
```

If the CLI is not installed, see: https://docs.databricks.com/dev-tools/cli/install

---

## Project Instructions

<!-- Add your project-specific instructions, coding conventions, or notes here -->
