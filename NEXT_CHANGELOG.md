# NEXT CHANGELOG

## Release v0.300.0

### CLI

* JSON output (`--output json`) now uses standard `encoding/json` spacing (`"key": "value"` instead of `"key":"value"`).

### Bundles

### Dependency updates
