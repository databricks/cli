version: 1.1

source: "WITH foo AS (SELECT * FROM main.sales.orders) SELECT * FROM foo"
filter: region = 'WEST'
dimensions:
  - name: month
    expr: MONTH(o_orderdate)
measures:
  - name: count_orders
    expr: COUNT(*)
