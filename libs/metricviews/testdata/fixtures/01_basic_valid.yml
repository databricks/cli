version: 1.1

source: main.sales.orders
filter: region = 'WEST'
dimensions:
  - name: month
    expr: MONTH(o_orderdate)
measures:
  - name: count_orders
    expr: COUNT(*)
  - name: running_count_orders
    expr: SUM(COUNT(*)) OVER (ORDER BY month)
