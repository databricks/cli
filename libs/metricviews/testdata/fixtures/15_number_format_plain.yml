version: 1.1

source: main.sales.orders
measures:
  - name: total_revenue
    expr: SUM(o_totalprice)
    format:
      type: number
      decimal_places:
        type: exact
        places: 2
      abbreviation: compact
