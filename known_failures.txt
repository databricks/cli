# List of test cases that will not cause integration test to fail.
# The test will be still run but failure is silented. See tools/testrunner on main branch.
#
# Format is two words: package testname
#
# Each can be replaced by "*" (without quotes to match everything)
# * TestAccept/selftest/basic  # matches this test name across any package
# libs/telemetry *             # matches all test cases inside libs/telemetry package
#
# The above examples are non-recursive. If package name or test name ends with a /, it acts as a prefix:
# acceptance TestAccept/bundle/resources/jobs/  # jobs undergoing maintenance
#
# We also support wildcards inside the path
# acceptance TestAccept/bundle/*/DMS=true/    # all DMS=true permutations
# acceptance TestAccept/bundle/*/something    # all test paths ending in /something

# SSH test is flaky due to concurrent test suites (Linux/Windows) running on same cluster.
# Current implementation uses cluster-id as server identifier, causing SIGTERM conflicts.
# Fix exists in serverless connection mode (not yet merged).
# Slack: https://databricks.slack.com/archives/C03AV2J5FSR/p1767964913136479
acceptance TestAccept/ssh/connection

# DMS failures due to missing enum values
acceptance TestAccept/bundle/resources/postgres_snapshot_schedules/basic/DATABRICKS_BUNDLE_ENGINE=direct/DMS=true
acceptance TestAccept/bundle/resources/postgres_snapshot_schedules/out-of-band/DATABRICKS_BUNDLE_ENGINE=direct/DMS=true
acceptance TestAccept/bundle/resources/model_services/lifecycle/DATABRICKS_BUNDLE_ENGINE=direct/DMS=true
# DMS failures due to workspace clean-up issues
# (enable only after # https://github.com/databricks/cli/pull/6627 is merged)
# acceptance TestAccept/bundle/*/DMS=true/

# Need to convert to acceptance tests; terraform registry is not accessible from CI.
* TestSparkJarTaskDeployAndRunOnVolumes
* TestSparkJarTaskDeployAndRunOnWorkspace
* TestGenerateFromExistingJobAndDeploy
