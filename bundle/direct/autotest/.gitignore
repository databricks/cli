# The full report moves whenever a passing row moves; output/<type>.txt is the one that
# records findings and is worth diffing. Both the fake server and a real workspace are held
# to that single golden.
output/*.full.txt
