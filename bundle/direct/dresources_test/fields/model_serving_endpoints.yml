# Value library for model serving endpoints. See ../README.md.
#
# Cloud is off: creating an endpoint provisions serving compute.
cloud: false

skip:
  # The inference table name is only accepted alongside a catalog and schema that exist
  # and are writable, which one-field-at-a-time cannot arrange.
  telemetry_config.inference_table_config.name: needs a writable catalog and schema
