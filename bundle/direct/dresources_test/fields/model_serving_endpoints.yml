# Value library for model serving endpoints. See ../README.md.
#
# Cloud is off: creating an endpoint provisions serving compute.
cloud: false

skip:
  # The telemetry block is only accepted as a coherent whole, against a catalog and
  # schema that exist and are writable, which one-field-at-a-time cannot arrange.
  telemetry_config.*: needs a writable catalog and schema
