backend_defaults:
  # A policy authored with policy_family_id and no definition gets its definition
  # computed from the policy family by the backend and returned on read. Config
  # leaves definition empty, so old/new are nil and backend_defaults skips the
  # server-computed remote value; without this every deploy sees drift and re-Edits.
  - field: definition
