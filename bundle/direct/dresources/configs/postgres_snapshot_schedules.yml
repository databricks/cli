provided_id_fields:
  # branch composes the schedule's hierarchical name
  # ("{branch}/snapshot-schedule"); changing it targets a different branch's
  # schedule, so it recreates (delete + create).
  - field: branch
    reason: id_field
