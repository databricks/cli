bundle:
  name: pipeline_glob_paths

include:
  - resources/pipeline.yml

targets:
  default:
    default: true

  error:
    default: false

    resources:
      pipelines:
        nyc_taxi_pipeline:
          libraries:
            - notebook:
                path: ./non-existent
