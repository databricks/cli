# {{.project_name}}

Generated by `databricks ucm init brownfield`.

This project is a stub. Populate it by scanning live UC state:

```bash
databricks ucm generate{{if .metastore_name}} --metastore-name {{.metastore_name}}{{end}}
databricks ucm validate
databricks ucm plan
```

See https://docs.databricks.com/en/dev-tools/ucm/index.html for the full
reference.
