# {{.project_name}}

Generated by `databricks ucm init default`.

## Structure

- `ucm.yml` — declarative UC configuration. Starts with one catalog
  (`{{.catalog_name}}`), one schema (`{{.schema_name}}`) and a read grant to
  `{{.principal}}`.

## Workflow

```bash
databricks ucm validate
databricks ucm plan
databricks ucm deploy
```

See https://docs.databricks.com/en/dev-tools/ucm/index.html for the full
reference.
