# {{.project_name}}

Generated by `databricks ucm init multienv`.

## Targets

- `dev` (default) — catalog `{{.catalog_prefix}}_dev`
- `staging` — catalog `{{.catalog_prefix}}_staging`
- `prod` — catalog `{{.catalog_prefix}}_prod`

## Workflow

```bash
databricks ucm validate --target dev
databricks ucm plan --target staging
databricks ucm deploy --target prod
```

See https://docs.databricks.com/en/dev-tools/ucm/index.html for the full
reference.
