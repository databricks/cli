* `ssh connect` sessions now survive an unexpected connection drop. When the tunnel websocket is reset mid-session, the CLI reattaches to the running session and replays the bytes that were lost, so the shell and everything running in it stay intact instead of the session ending with `failed to read from websocket`. Requires an SSH server started by a CLI that supports it; against an older server the connection behaves as before.
