* `bundle validate` now fails early with a clear error when a `sql_warehouse` is missing a `name` or a grant is missing a `principal`, instead of passing validation and failing later at deploy ([#5818](https://github.com/databricks/cli/pull/5818)).
