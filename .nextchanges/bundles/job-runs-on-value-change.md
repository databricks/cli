direct: `resources.job_runs` can set `lifecycle.triggers.on_value_change` to an interpolated expression (for example `${resources.jobs.foo.id}`) to re-fire the run when that value changes or a watch is added. Resolved values are recorded as SHA-256 hashes. Removing a watch does not recreate the existing run.
