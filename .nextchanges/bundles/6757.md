* direct: Fix a spurious `permissions` update reported on every plan and deploy when a service principal is declared under `user_name` and the Permissions API returns it as `service_principal_name`. Keyed-slice elements in `bundle plan --output json` are now addressed by value as `[='value']` instead of `[field='value']`. ([#6757](https://github.com/databricks/cli/pull/6757))
