Fixed `telemetry_config` on model serving endpoints never converging on the direct engine: the backend provisions a telemetry profile for the endpoint and returns its `telemetry_profile_id` on GET, which was reported as drift, so every `bundle plan` after a deploy showed the endpoint as a perpetual update. Changes to `telemetry_config` are now applied through the serving endpoints telemetry configuration API instead of being dropped.
