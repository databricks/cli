direct: the experimental `job_runs` resource now waits for the run to finish and fails the deploy if it does not succeed, so other resources can reference the run's outcome. A retried deploy rejoins the run it already triggered instead of starting a duplicate; set `rerun_token` to a new value to run the job again.
