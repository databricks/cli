direct: the experimental `job_runs` resource now waits for the run to finish, fails the deploy on a non-success result, and dedupes runs on retries. Set `rerun_token` to force a fresh run, `wait_for_completion: false` to skip waiting, or `timeout` to bound the wait. A deploy that dedupes onto a run triggered earlier says so instead of reporting it as fresh.
