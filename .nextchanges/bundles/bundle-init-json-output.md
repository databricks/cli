Added JSON output to `bundle init`. Running `databricks bundle init <template> -o json` now reports the template name, the resolved output directory, and the bundle roots the template materialized (the directories that received a bundle configuration file), relative to the output directory. This lets callers that pass `--output-dir` learn which subdirectory became the bundle root instead of assuming it is named after the project. The default text output is unchanged.
