* `bundle destroy` and job-run recreations no longer delete the triggered job run, since a run is a historical record; the run is left untouched and only dropped from bundle state. ([#6672](https://github.com/databricks/cli/pull/6672))
