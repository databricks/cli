Fixed a Lakebase (`postgres_*`) deploy failing with `Field '...' is in update_mask but not provided in request` when a nested block such as `default_endpoint_settings` was added to the configuration and left some of its fields unset.
